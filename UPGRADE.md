# Αναβάθμιση WHMCS — τι έχουμε αλλάξει και τι πρέπει να ελεγχθεί μετά

**Τρέχουσα έκδοση:** WHMCS 9.0.6-release.1
**PHP:** 8.3 (`/opt/plesk/php/8.3/bin/php`)
**Πρότυπο πελάτη:** `horn` (τρίτου κατασκευαστή) · **Φόρμα παραγγελίας:** `horn`
**Διαδρομή admin:** `cloudonadminpanel` (`customadminpath` στο `configuration.php`)
**Κώδικας:** GitHub private — `delispanos-png/Project-Management-`, κλάδος `master`

---

> **Τεχνικό εγχειρίδιο**: για την αρχιτεκτονική, το μοντέλο δεδομένων, τα δικαιώματα και
> τις παγίδες του κώδικα, δες **`TECHNICAL.md`**. Εδώ είναι το ιστορικό των αλλαγών στο
> WHMCS και η λίστα ελέγχου της αναβάθμισης.

## 1. Η πιο σημαντική πληροφορία

**Δεν έχει τροποποιηθεί κανένα αρχείο πυρήνα του WHMCS.** Επαληθεύτηκε με **τρεις
ανεξάρτητες μεθόδους** (30/07/2026), γιατί το git ξεκινά 19/07/2026 και δεν καλύπτει
από μόνο του ό,τι έγινε νωρίτερα:

1. **Ιστορικό git** από το baseline μέχρι σήμερα → καμία αλλαγή σε `includes/`
   (πλην `hooks/`), `cloudonadminpanel/`, `vendor/`, `resources/`, php ρίζας.
2. **Αναζήτηση δικών μας σημαδιών** (ελληνικά σχόλια, `cnp_`, `hz_`, `CloudOn`) μέσα
   στους φακέλους πυρήνα → **μηδέν ευρήματα**.
3. **Χρονοσημάνσεις**: όλα τα αρχεία πυρήνα φέρουν **07/07/2026** (ημερομηνία
   εγκατάστασης 9.0.6). Μόνο τρεις εξαιρέσεις, όλες εξηγημένες:
   `terms.php` και `gdpr.php` (δικές μας σελίδες του 2024, δεν τις πειράζει η
   αναβάθμιση) και `configuration.php`.

Επίσης επαληθεύτηκε ότι **δεν πειράχτηκαν**: τα βασικά `lang/*.php` (μόνο
`lang/overrides/` χρησιμοποιούνται — σωστό), τα stock πρότυπα `six` /
`twenty-one`, οι stock φόρμες παραγγελίας, και κανένα stock module.

**Μία μόνο εκκρεμότητα εντοπίστηκε:** το `cloudonadminpanel/templates/blend-new/nav.tpl`
είναι τροποποιημένο αντίγραφο του admin προτύπου. **Δεν χρησιμοποιείται από κανέναν
διαχειριστή** (όλοι είναι σε `blend`, `AdminTemplate` κενό), οπότε δεν είναι κίνδυνος —
αλλά αν κάποτε ενεργοποιηθεί, να ξέρεις ότι είναι δική μας παραλλαγή.

Πρακτικά αυτό σημαίνει ότι **η αναβάθμιση δεν θα σβήσει λειτουργικότητα** — θα
σβήσει μόνο ό,τι πατάει σε συμβάσεις που ενδέχεται να αλλάξουν (ονόματα hooks,
συναρτήσεις, δομή προτύπου).

---

## 2. Τι επιβιώνει και τι κινδυνεύει

| Περιοχή | Επιβιώνει; | Κίνδυνος |
|---|---|---|
| `includes/hooks/*.php` | ✅ τα αρχεία μένουν | ⚠️ **μπορεί να αλλάξουν ονόματα/παράμετροι hooks** |
| `modules/gateways/vivapayments*` | ✅ | ⚠️ αλλαγές στο API των gateway modules |
| `modules/servers/*`, `modules/addons/*` (δικά μας) | ✅ | ⚠️ αλλαγές στο API των modules |
| `lang/overrides/*.php` | ✅ | χαμηλός |
| `templates/cloudon/**` (δικό μας fork) | ✅ | χαμηλός — κανείς δεν γράφει επάνω του |
| `templates/horn/**` (αναφορά, ανενεργό) | ✅ | κανένας |
| Δικοί μας φάκελοι ρίζας (`projectmanagement/`, `remote/`, `project/`) | ✅ | κανένας |
| Πίνακες `mod_*` στη βάση | ✅ | κανένας |
| Ρυθμίσεις στη βάση (gateways, αρίθμηση) | ✅ | ⚠️ μπορεί να επανέλθουν προεπιλογές |

**Ο κίνδυνος του προτύπου λύθηκε στις 30/07/2026** με fork σε `templates/cloudon`.
Πριν από αυτό, μια ενημέρωση του εμπορικού `horn` θα έσβηνε 23 δικά μας αρχεία.
Δες την ενότητα 4.

---

## 3. Τι έχουμε προσθέσει — αναλυτικά

### 3.1 Hooks (`includes/hooks/`)

Δικά μας αρχεία και τι θα σπάσει αν αλλάξει το hook που χρησιμοποιούν:

| Αρχείο | Hooks που χρησιμοποιεί | Τι κάνει | Αν σπάσει |
|---|---|---|---|
| `paypal_surcharge.php` | `InvoiceChangeGateway`, `InvoiceCreated`, `ClientAreaFooterOutput` | Χρέωση διεκπεραίωσης PayPal (5,4% + 0,35 € με αναγωγή) | Χάνεται η ανάκτηση προμήθειας PayPal — **οικονομική διαρροή** |
| `viva_reconcile.php` | `ClientAreaPage`, `AfterCronJob` | Άμεσος έλεγχος & συμφωνία πληρωμών Viva | **Πληρωμές δεν καταχωρούνται** — κρίσιμο |
| `cart_badge.php` | `ClientAreaPage`, `ClientAreaPageCart`, `ClientAreaPageHome`, `ClientAreaPageLogin` | Μετρητής προϊόντων στο εικονίδιο καλαθιού | Απλώς δεν φαίνεται ο αριθμός |
| `hz_home_shortcuts.php` | `ClientAreaSecondarySidebar` | Συντομεύσεις αρχικής | Αισθητικό |
| `hz_dashboard_panel.php`, `hz_dashboard_data.php` | `ClientAreaHomepagePanels` κ.ά. | Πίνακας ελέγχου πελάτη | Αισθητικό |
| `hz_auto_accept_orders.php` | `AfterShoppingCartCheckout` | Αυτόματη αποδοχή παραγγελιών | Παραγγελίες μένουν σε αναμονή |
| `hz_terminate_on_status.php` | αλλαγή κατάστασης υπηρεσίας | Τερματισμός υπηρεσιών | Υπηρεσίες δεν τερματίζονται |
| `hz_ticket_priority.php`, `hz_menu_availability.php`, `hz_location_filter.php`, `hz_service_icons.php`, `hz_homepage_categories.php`, `hz_mobile_css.php`, `hz_a11y_forms.php`, `hz_nav_a11y.php` | διάφορα ClientArea | Προσαρμογές εμφάνισης/ροής | Αισθητικό |
| `mask_admin_passwords.php` | admin | Απόκρυψη κωδικών στο admin | Ασφάλεια — εμφανίζονται κωδικοί |

**`example.php`, `index.php`, `zz_debug_productsave.php`** δεν είναι δικά μας ή
είναι διαγνωστικά· μπορούν να αγνοηθούν.

### 3.2 Modules που φτιάξαμε

| Module | Τύπος | Πίνακες βάσης | Σημείωση |
|---|---|---|---|
| `vivapayments` | gateway | `mod_viva_orders`, `mod_viva_tokens`, `mod_viva_log` | Πλήρες κύκλωμα Viva Smart Checkout |
| `scaleway` | server + addon | `mod_scaleway_*` | Φτιαγμένο, **δεν έχει δοκιμαστεί ζωντανά** |
| `hetznercloud`, `hetznerstorage` | server + addon | `mod_hetzner_*` | Ενεργό σε παραγωγή |
| `cloudonprojects` | addon | `mod_cpm_*` (60+ πίνακες) | Το Project Management |
| `cloudonprojects` → Προσλήψεις | addon | `mod_cpm_cv_jobs`, `mod_cpm_cv`, **`mod_cpm_cv_job_views`** | Αγγελίες, υποψήφιοι, **επισκεψιμότητα** |
| `supportcontracts` | addon | `mod_supportcontracts_*` | Τράπεζα χρόνου υποστήριξης |
| `gooddaysync` | addon | `mod_gooddaysync_*` | Συγχρονισμός GoodDay |
| `staffboard`, `servicesfee`, `ai_copilot`, `bulkpricingupdater` | addon | διάφοροι | |

### 3.3 Αρχεία γλώσσας

`lang/overrides/greek.php` και `lang/overrides/english.php` — περιέχουν κλειδιά με
πρόθεμα `cnp_`:

- `cnp_remote_nav` — «Απομακρυσμένη υποστήριξη» στο μενού
- `cnp_afm_*` — άντληση στοιχείων από ΑΑΔΕ στη φόρμα εγγραφής
- `cnp_cf_<id>` — μεταφράσεις custom fields (το WHMCS **δεν** τα μεταφράζει μόνο του)

⚠️ **Μην γράψεις ποτέ `$_LANG['customfield']['1']`** — το κλειδί `customfield`
προϋπάρχει ως **string** και η ανάθεση δείκτη προκαλεί **fatal error** (λευκή σελίδα).

### 3.4 Δικοί μας φάκελοι εκτός WHMCS

| Φάκελος | Τι είναι |
|---|---|
| `projectmanagement/` | Η εφαρμογή Project Management (SPA) |
| `project/` | Alias που σερβίρει το παραπάνω |
| `remote/` | Δημόσια σελίδα λήψης εργαλείου απομακρυσμένης υποστήριξης |
| `cloudonadminpanel/` | Το admin (μετονομασμένο) |
| `sys_cron_k7m2/` | Ο φάκελος cron (μετονομασμένος) |
| `tpl_cache_k7m2/` | Cache προτύπων (μετονομασμένο) |

⚠️ Οι μετονομασμένοι φάκελοι (`cloudonadminpanel`, `sys_cron_k7m2`, `tpl_cache_k7m2`)
ορίζονται στο `configuration.php`. **Μετά την αναβάθμιση επιβεβαίωσε ότι το
`configuration.php` δεν αντικαταστάθηκε.**

---

### 3.5 Δημόσια σελίδα καριέρας & επισκεψιμότητα

| Αρχείο | Ρόλος |
|---|---|
| `projectmanagement/apply.php` | Η δημόσια σελίδα αγγελιών (EL/EN) |
| `modules/addons/cloudonprojects/lib/JobViews.php` | Καταγραφή & στατιστικά επισκεψιμότητας |
| `projectmanagement/api.php` → `cv_job_views` | API στατιστικών |
| `projectmanagement/views4.js` → `renderTrafficPanel()` | Η καρτέλα «Επισκεψιμότητα» |

Η μέτρηση γίνεται σε τρία σημεία του `apply.php`:
`?trk=<id>` (beacon από το modal), προβολή landing (`job_id = 0`), άνοιγμα φόρμας.
**Δεν αποθηκεύεται IP** — μόνο ανώνυμο ημερήσιο αποτύπωμα. Τα bots φιλτράρονται.

### 3.9 CloudOn Project Manager — προσθήκες Αυγούστου 2026

Ο addon `cloudonprojects` **δεν είναι μέρος του WHMCS** και δεν πειράζεται από
την αναβάθμιση· εξαρτάται όμως από πίνακες του WHMCS. Αν αλλάξει κάτι εκεί,
σπάει σιωπηλά.

| Τι προστέθηκε | Πού | Εξάρτηση από WHMCS | Αν σπάσει |
|---|---|---|---|
| `lib/Deadlines.php` — προειδοποίηση & κλιμάκωση προθεσμιών | καλείται από `crons/pulse.php` (κάθε 10΄, `/etc/cron.d/cloudonprojects`) | `tbltickets.flag`, `tblquotes.validuntil` | Χάνονται οι ειδοποιήσεις προθεσμιών — καμία ζημιά στα δεδομένα |
| Υιοθέτηση προσφορών `Db::adoptQuotes()` / `syncOfferTracking()` | `lib/Db.php`, τρέχει σε κάθε άνοιγμα της οθόνης Προσφορών | `tblquotes` (`datesent`, `dateaccepted`, `stage`, `validuntil`, `total`) | Η οθόνη Προσφορών δεν βλέπει νέα quotes |
| Ουρά ημέρας & καθοδήγηση με αναφορές | `projectmanagement/api.php` (`myday`) | `tbltickets`, `tblticketreplies`, `mod_supportcontracts_tickets` | Άδεια κάρτα στη «Μέρα μου» |
| Ενσωματωμένο ticket στην εργασία | `api.php` (`task`), `app.js` | `tbltickets`, `tblticketreplies`, `tblticketdepartments` | Δεν φαίνεται η συνομιλία μέσα στην εργασία |

**Νέα σχήματα (δικά μας, δεν τα αγγίζει η αναβάθμιση):**

| Πίνακας / στήλη | Ρόλος |
|---|---|
| `mod_cpm_deadline_alerts` | Ποια ειδοποίηση προθεσμίας στάλθηκε σε ποιον· UNIQUE `(kind, ref_id, level, admin_id, sent_on)`. **Χωρίς αυτό ο cron των 10΄ στέλνει 144 φορές τη μέρα το ίδιο.** |
| `mod_cpm_projects.manager_id` | Υπεύθυνος έργου — σε αυτόν κλιμακώνουν οι χαμένες προθεσμίες |
| `mod_cpm_tasks.completed_note`, `completed_by` | Δυο λόγια για το πώς έκλεισε η εργασία |
| `mod_cpm_offers.sent_at`, `replied_at`, `reply`, `followup_date`, `followup_note`, `followup_done_on` | Παρακολούθηση προσφοράς |
| `mod_cpm_interactions.offer_id` | Επικοινωνία δεμένη σε προσφορά |
| `mod_cpm_tasks.dept_id` | Σε ποιο **department** ανατίθεται η εργασία → `tblticketdepartments.id`. **Η ιεραρχία είναι πελάτης → έργο → εργασίες, και η κάθε εργασία ανατίθεται σε μία ομάδα** — γι' αυτό το department κρέμεται στην εργασία, όχι στο έργο. Νέες εργασίες το παίρνουν μόνες τους (από το ticket ή από το `deptid` του project) στο `Db::saveTask()`. **Δεν υπάρχει δεύτερο μητρώο τμημάτων**: τα departments είναι αυτά του WHMCS, τα ίδια που επιλέγει ο πελάτης όταν ανοίγει ticket. Χρώμα/σύμβολο παράγονται ντετερμινιστικά στη `cnp_depts()`. |

Όλα δημιουργούνται ιδεμποτικά από το `Db::schema()` — τρέχει μόνο του.

**Πλοήγηση διπλής κατεύθυνσης** (Αύγουστος 2026): πελάτης ⇄ έργο ⇄ εργασία ⇄ department ⇄ ticket.
Οθόνες `#/units` (κατάλογος ομάδων) και `#/unit/<id>` (φορτίο μιας ομάδας, ανά πελάτη & έργο) —
τα routes κράτησαν το παλιό όνομα, τα δεδομένα έρχονται από `depts_load` / `dept_view`.
Λωρίδα ομάδων στην κεφαλίδα του board (`board.meta.deptSplit`), ψίχουλα στο drawer
(`task.owner` / `task.project` / `task.depts`), και ομαδοποίηση των έργων ανά πελάτη στο `#/projects`.
**Εξάρτηση:** ο router αναγνωρίζει πλέον αλλαγή id στην ίδια οθόνη (`S.viewArg`) — χωρίς αυτό
το `#/unit/2 → #/unit/3` δεν ξαναφόρτωνε.

**Προσοχή στην αναβάθμιση:** ο πίνακας `tblticketdepartments` είναι πλέον **εξάρτηση του PM**, όχι
μόνο του ticket system. Αν σβηστεί department που έχει εργασίες, αυτές μένουν με `dept_id` που δεν
δείχνει πουθενά και εμφανίζονται ως «χωρίς ομάδα». Η οθόνη Ρυθμίσεις → Departments ήδη μπλοκάρει
διαγραφή όταν υπάρχουν tickets — **δεν** ελέγχει ακόμη εργασίες PM.

**Ρόλοι στην εργασία** — τρία διαφορετικά πράγματα που μπερδεύονταν:
`dept_id` = σε ποια **ομάδα ανήκει**· `assignee` = **ποιος την εκτελεί** (άνθρωπος)·
`action_user` = σε ποιον είναι η μπάλα τώρα. Η λίστα ανάθεσης ομαδοποιείται με βάση το
`tbladmins.supportdepts` του WHMCS, ώστε να προτείνονται πρώτα τα μέλη της ομάδας.

**Φόρμα έργου:** το έργο ανήκει στον **πελάτη**, όχι σε ομάδα — το πεδίο department αφαιρέθηκε
από τα έργα πελατών (μένει μόνο στα legacy `kind=dept`) και το «Γονικό» δείχνει πλέον μόνο άλλα
έργα πελατών. Η αποθήκευση **μπλοκάρει** έργο πελάτη χωρίς `clientid`.

**Επιλογή πελάτη — `clientAuto()` (views2.js):** το `<datalist>` αντικαταστάθηκε από πραγματικό
combobox (πάνελ αποτελεσμάτων, πλήκτρα ↑/↓/Enter/Esc, κουμπί καθαρισμού). Το `<datalist>` δεχόταν
ελεύθερο κείμενο, οπότε γραφή χωρίς επιλογή άφηνε το hidden id κενό και η εγγραφή αποθηκευόταν
σιωπηλά χωρίς πελάτη. Πλέον η τιμή μπαίνει **μόνο** με επιλογή, το πεδίο βάφεται πράσινο/κόκκινο,
και στο blur δένεται μόνο αν το κείμενο ταιριάζει σε ακριβώς έναν. Ίδια υπογραφή — διορθώνονται
μαζί και τα **έξι** σημεία που την καλούν (έργα, προσφορές, CRM, ημερολόγιο, απομακρυσμένες, vault).
Το `client_search` επιστρέφει τώρα και `name` / `email` / `status`.
Νέα ενέργεια `project_delete` (καθαρίζει 12 συνδεδεμένους πίνακες· ζητά ρητό `withTasks`).
Η φόρμα έχει πλέον και κάρτα **«Εργασίες του έργου»**: λωρίδα προόδου ανά ομάδα, λίστα με
ολοκληρωμένες σε `<details>`, και γρήγορη προσθήκη όπου η **ομάδα είναι υποχρεωτική** — αλλιώς
η εργασία θα γεννιόταν αζήτητη (το `Db::saveTask()` δεν έχει τι να κληρονομήσει σε έργο πελάτη).
Μετά τη δημιουργία νέου έργου η φόρμα οδηγεί κατευθείαν στο board του.

**Δικαιώματα μέσω ομάδας** (Αύγουστος 2026): `mod_cpm_teams.areas` (csv) κρατάει σε ποια κυκλώματα
έχει πρόσβαση η ομάδα· `cnp_admin_areas()` επιστρέφει την **ένωση** των ομάδων του χειριστή + του
προσωπικού pref `areas`. Οι περιοχές ορίζονται μία φορά στη `cnp_area_defs()` (support / projects /
sales / hr / admin) — τη διαβάζουν και ο έλεγχος και η οθόνη, ώστε να μην αποκλίνουν.

**Μενού ανά κύκλωμα** (Σεπτέμβριος 2026). Οι ενότητες ήταν ανακατεμένες: η «Εργασία» είχε μαζί τα
προσωπικά μου και τη συνεννόηση της ομάδας, ο «Πελάτης 360°» ζούσε στην Υποστήριξη ενώ είναι η
καρτέλα του πελάτη, και τα «Έργα» ήταν 7 στοιχεία χωρίς σειρά. Νέες ενότητες, καθεμιά με μία
ερώτηση που απαντά (φαίνεται ως tooltip στον τίτλο):

| Ενότητα | Τι ζει μέσα |
|---|---|
| **Τα δικά μου** | Η μέρα μου, Το πλάνο μου, Ο χρόνος μου, Βιβλιοθήκη, Κωδικοί, **Το προφίλ μου** |
| **Πελάτες** | Πελάτης 360° *(support)*, CRM & leads, Προσφορές *(sales)* |
| **Υποστήριξη** | Tickets, Βάση γνώσης |
| **Έργα & υλοποιήσεις** | Έργα πελατών, Board, Λίστα εργασιών, Χρονοδιάγραμμα, Modules, Departments |
| **Η ομάδα** | Chat, Ημερολόγιο, Standup, Απομακρυσμένες, Ομάδες *(admin)*, Απόδοση *(reports)* |
| **Διοίκηση** | Πλάνο ημέρας, KPI, Ανάλυση ριζών *(reports)* · Κερδοφορία, Συμφωνία, Αναστολές *(finance)* · Ρυθμίσεις *(admin)* |

**Κάθε στοιχείο φέρνει το δικό του δικαίωμα** (`A(area, items)`), όχι η ενότητα — έτσι μια ενότητα
μπορεί να ανήκει σε δύο κυκλώματα (Πελάτες = support + sales) και εμφανίζεται με ό,τι δικαιούται ο
καθένας· ενότητα που μένει άδεια δεν εμφανίζεται καθόλου. Το «Το προφίλ μου» μπήκε στο μενού — πριν
ήταν προσβάσιμο μόνο από το avatar.

**«Μόνο για διαχειριστές» → το πραγματικό μήνοιμα.** Μετά το σπάσιμο της Διοίκησης σε τρία, η φράση
ήταν λάθος: ένας project manager με `reports` **βλέπει** το KPI. Νέος βοηθός `cnpDenied(err)` δείχνει
το μήνυμα της πύλης («Δεν έχεις πρόσβαση στο κύκλωμα «Αναφορές & απόδοση»») και εξηγεί ότι τα
δικαιώματα δίνονται από τις ομάδες.

**Το κύκλωμα «Διοίκηση» σπάστηκε σε τρία** (Αύγουστος 2026), γιατί ήταν όλα-ή-τίποτα: για να δώσεις
KPI σε έναν project manager έδινες μαζί κερδοφορία και ρυθμίσεις.
`reports` = Πλάνο ημέρας, Ανάλυση ριζών, KPI, Απόδοση (+`agenda`, `topstats`) ·
`finance` = Κερδοφορία, Συμφωνία πληρωμών, Λογιστικός έλεγχος, Αναστολές ·
`admin` = Ομάδες, Χρήστες, Ρυθμίσεις, automations, πακέτα, storage.
Η μετάπτωση δίνει και τα τρία σε όποια ομάδα/pref είχε το παλιό `admin`, ώστε να μη χαθεί πρόσβαση.
Το μενού «Διοίκηση» συνθέτεται πλέον ανά ενότητα (`admItems` στο `renderShell`).
**Οι εσωτερικοί έλεγχοι `if (!$FULL)` στα `kpi` / `perf` / `triage` / `rootcause` αφαιρέθηκαν** —
τη θέση τους πήρε η πύλη περιοχών· διαφορετικά το δικαίωμα `reports` δεν θα είχε κανένα αποτέλεσμα.
Στα `finance` και `admin` οι έλεγχοι `$FULL` **παραμένουν** ως δεύτερος φράχτης.

**Έλεγχος στον server:** νέα πύλη πριν το `switch ($action)` — `cnp_action_area()` χαρτογραφεί
**178 από τις 255** ενέργειες σε περιοχή και κόβει με 403. Είναι **allow-by-default**: ενέργεια που
δεν αναφέρεται δεν μπλοκάρεται (δεν κλειδώνει κατά λάθος λειτουργία που δεν καταγράφηκε). Οι
υπάρχοντες έλεγχοι `$FULL` **μένουν** — η πύλη είναι επιπλέον φράχτης. Οι 77 ακάλυπτες είναι τα
προσωπικά/κοινά κυκλώματα (μέρα μου, ημερολόγιο, chat, βιβλιοθήκη, κωδικοί, πλάνο, αρχεία, προφίλ,
remote), που δεν έχουν guard ούτε στο μενού.
**Αν προστεθεί νέα ενέργεια σε κύκλωμα, πρόσθεσέ την και στον χάρτη** — αλλιώς μένει ελεύθερη.

Οθόνη **Ομάδες & δικαιώματα**: drawer επεξεργασίας (όνομα, χρώμα, περιγραφή, δικαιώματα, μέλη με
ρόλο/επικεφαλής, διαγραφή) — πριν η ομάδα μπορούσε μόνο να δημιουργηθεί και να διαγραφεί.
Ενέργεια `teams_seed`: μία ομάδα ανά department με τα μέλη του από το `tbladmins.supportdepts` και
προτεινόμενο κύκλωμα από το όνομα· παραλείπει ομάδα που υπάρχει ήδη με το ίδιο όνομα.

**Ομάδα ≠ department.** `mod_cpm_team_depts` (πολλά-προς-πολλά): το **department** είναι πού
απευθύνεται το αίτημα (Support/Sales/Accounting, τα ticket departments του WHMCS)· η **ομάδα** είναι
ειδικότητα ανθρώπων (Λογιστές, DevOps, R&D, Αναλυτές). Ένα department το εξυπηρετούν πολλές ομάδες
και μια ομάδα εξυπηρετεί πολλά departments. Η `cnp_depts()` βγάζει πλέον τα μέλη ενός department από
τις ομάδες που το εξυπηρετούν, με **εφεδρεία** το `tbladmins.supportdepts` όσο δεν έχουν οριστεί
ομάδες — έτσι η ανάθεση δεν αδειάζει στη μετάβαση. Το `teams_seed` αφαιρέθηκε: δημιουργούσε ομάδες
αντίγραφα των departments και δίδασκε το λάθος μοντέλο.

**Ορατότητα έργου ≠ δικαίωμα κυκλώματος.** Το `Db::visibleProjectIds()` επιστρέφει την ένωση
`mod_cpm_project_members` (ονομαστικά) + `mod_cpm_project_teams` (μέσω ομάδας)· `null` μόνο για
πλήρη πρόσβαση. **Άδεια και τα δύο σημαίνει ότι κανένας μη-διαχειριστής δεν βλέπει το έργο** — όχι
«ανοιχτό σε όλους». Η φόρμα του έργου το λέει πλέον ρητά με ζωντανή ένδειξη («⚠ Μόνο οι διαχειριστές
θα βλέπουν αυτό το έργο»). Τρίτος δρόμος: το `Db::canSeeTask()` επιτρέπει την εργασία στον
`assignee` της ακόμη κι όταν το έργο δεν είναι ορατό.

Η οθόνη Ομάδες επισημαίνει **περιττές συμμετοχές**: μέλος του οποίου οι άλλες ομάδες δίνουν ήδη ό,τι
δίνει αυτή («δεν προσθέτει δικαιώματα»), ομάδα της οποίας κανένα μέλος δεν κερδίζει τίποτα, και
μέλος με ρόλο πλήρους πρόσβασης στο WHMCS (τα δικαιώματα της ομάδας δεν τον αφορούν). Χωρίς αυτό,
ομάδες που επικαλύπτονται πλήρως μοιάζουν ενεργές ενώ δεν κάνουν τίποτα.

**Μία πηγή δικαιωμάτων.** Οι `Ρυθμίσεις → Χρήστες` είχαν δεύτερο σετ checkbox («Ειδικότητες») που
έγραφε το προσωπικό pref `areas` παράλληλα με τις Ομάδες — δύο σημεία για το ίδιο πράγμα, και το ένα
χωρίς καν το κύκλωμα `admin`. Καταργήθηκαν: η οθόνη δείχνει πλέον **μόνο** σε ποιες ομάδες ανήκει ο
χειριστής και τι ισχύει. Το `user_areas_save` μένει για να **καθαρίζει** παλιές προσωπικές εξαιρέσεις
(κουμπί ✕), όχι για να δίνει δικαιώματα.

Το μενού «Διοίκηση» ακολουθεί πλέον το δικαίωμα `admin` αντί για το `me.full`.
Ρύθμιση `strict_areas` (κλειστή): όσο είναι κλειστή, χειριστής χωρίς ομάδα **και** χωρίς pref
παίρνει την ιστορική προεπιλογή `support,projects,sales`. Άναψέ τη αφού μπουν όλοι σε ομάδες.

**Ορολογία — μία λέξη, μία έννοια.** Μετά τον διαχωρισμό ομάδας/department έγινε σάρωση σε όλα τα
αρχεία (app.js, views2-5.js, help.js, api.php, cloudonprojects.php, Db.php): κάθε ετικέτα «ομάδα» που
εννοούσε department μετονομάστηκε. Η λέξη **ομάδα** σημαίνει πλέον αποκλειστικά `mod_cpm_teams`
(εκτός από τη γενική χρήση «η ομάδα» = το προσωπικό: φόρτος ομάδας, κοινή βιβλιοθήκη ομάδας, chat).
Ο οδηγός χρήσης (§22) ξαναγράφτηκε ώστε να εξηγεί τους τρεις ρόλους: department → ομάδα → άνθρωπος.
Η σελίδα Ομάδων στο **WHMCS admin** δείχνει πλέον read-only τα departments και τα κυκλώματα κάθε
ομάδας, με ρητή προειδοποίηση ότι ορίζονται στην εφαρμογή PM — ομάδα φτιαγμένη εκεί χωρίς αυτά δεν
δίνει δικαιώματα σε κανέναν.

**Εργασία χωρίς έργο.** Το `mod_cpm_tasks.project_id` έγινε **nullable** και τα τρία ψευτο-έργα
(#11 Support / #12 Sales / #13 Accounting Department) **καταργήθηκαν** — υπήρχαν μόνο επειδή η στήλη
ήταν NOT NULL. Οι 13 εργασίες τους κράτησαν το `dept_id` και έχασαν το έργο. Πλέον:
«έργο» σημαίνει πάντα δουλειά που παραδίδεται σε πελάτη· εργασία από ticket ανήκει **μόνο** σε
department. Τι άλλαξε στον κώδικα:

- **20 INNER JOIN** εργασία→έργο έγιναν `leftJoin` (Db.php ×5, api.php ×11, cloudonprojects.php ×3,
  Notify.php ×1). Τα joins από `recurring`/`expenses`/`project_teams` έμειναν INNER — εκεί το
  `project_id` είναι όντως υποχρεωτικό.
- Βοηθός `cnp_pn()` → «Χωρίς έργο» σε κάθε σημείο που το API στέλνει όνομα έργου (13 σημεία)·
  αντίστοιχη κάλυψη στις σελίδες του WHMCS admin και στο digest email.
- `Db::canSeeTask()`: εργασία χωρίς έργο τη βλέπει ο ανάδοχος, ο κάτοχος της μπάλας, οι full, και
  όποιος ανήκει σε **ομάδα που εξυπηρετεί το department** της.
- `hooks.php`, `api.php` και `cloudonprojects.php`: ticket → εργασία **χωρίς έργο**, με το `dept_id`
  του ticket. Η `Db::projectForDept()` δεν καλείται πλέον από πουθενά.
- `quick_task`: δέχεται έργο **ή** department (ένα από τα δύο αρκεί, κανένα δεν επιτρέπεται).
- `case 'board'`: 404 όταν το έργο δεν υπάρχει πια· `vBoard()` πέφτει στο πρώτο διαθέσιμο αντί να
  επιμένει σε id που χάθηκε.
- Drawer: χωρίς έργο δεν δείχνει σύνδεσμο board· οι υποψήφιες **εξαρτήσεις** έρχονται από το
  `dept_view` αντί για το board.

**Κλιμάκωση προθεσμιών με ομάδες.** Η `Deadlines::recipients()` ειδοποιεί πλέον και τους
**επικεφαλής των ομάδων** που εξυπηρετούν το department της εργασίας (ρόλος `lead`, email στο
επίπεδο `over`). Χωρίς αυτό, μια χαμένη προθεσμία σε ticket-εργασία έφτανε μόνο στον ανάδοχο —
δηλαδή σε αυτόν που ήδη την έχασε. Ο σύνδεσμος της ειδοποίησης δείχνει στο `#/unit/<dept>` όταν
δεν υπάρχει board.

**Αδήλωτο έργο = ανοιχτό, όχι αόρατο.** Το `visibleProjectIds()` επιστρέφει πλέον και τα έργα που
δεν έχουν **κανένα** μέλος και **καμία** ομάδα: θεωρούνται αδήλωτα, όχι κλειδωμένα. Πριν, ένα νέο
έργο ήταν αόρατο σε κάθε μη-διαχειριστή μέχρι να θυμηθεί κάποιος να ορίσει πρόσβαση — κάθε νέος
χειριστής έβλεπε άδειο board. Μόλις δηλωθεί έστω ένα μέλος ή μία ομάδα, το έργο περιορίζεται σε
αυτούς· η φόρμα το λέει ζωντανά.

**Ο φραγμός ΠΡΙΝ από τα δικαιώματά μας — ΔΥΟ πράγματα, και τα δύο του WHMCS:**

1. `tbladdonmodules` (`module=cloudonprojects`, `setting=access`) = οι **ρόλοι** που επιτρέπονται στο
   addon. Ήταν `1` (μόνο Full Administrator) → *«Access has not been given for your admin role group
   to access this addon module»*. Έγινε `1,3,4`.
2. `tbladminperms` **permid 46 = «Addon Modules»** = το δικαίωμα να ανοίξει καν το `addonmodules.php`.
   Έλειπε από Support Staffe και Accounting → `accessdenied.php?permid=46`, **πριν** φορτώσει το
   addon, οπότε ούτε το μήνυμα #1 δεν εμφανιζόταν. Προστέθηκε στους ρόλους 3 και 4.

Επαληθεύτηκε με **πραγματική είσοδο** προσωρινού χειριστή ρόλου Support Staffe:
`login.php → dologin.php (302) → addonmodules.php?pmlaunch=1 (302 → /project/?t=…) → app 200 →
api boot ✓`. Ο προσωρινός χειριστής διαγράφηκε.
**Κάθε νέος ρόλος WHMCS χρειάζεται ΚΑΙ τα δύο, αλλιώς οι χειριστές του είναι κλειδωμένοι έξω.**
Η οθόνη Ρυθμίσεις → Χρήστες ελέγχει πλέον **και τα δύο** και το εντοπίζει μόνη της: κόκκινη κάρτα με
τους ρόλους που λείπουν και κουμπί «Δώσε πρόσβαση» (ενέργεια `addon_access_grant`, που γράφει και τη
λίστα `access` και το permid 46), και σήμανση «δεν μπαίνει στην εφαρμογή» δίπλα σε κάθε χειριστή.

**Παγίδα του χάρτη περιοχών.** Τέσσερις ενέργειες ήταν λάθος ταξινομημένες γιατί τις καλούν οθόνες
**χωρίς** φραγμό στο μενού: `topstats` (σφυγμός πάνω μπάρας — κάθε φόρτωση), `agenda` (Standup),
`worknote_save` (Το πλάνο μου), `crm` (Επαφές). Έβγαλαν 403 σε κάθε χειριστή χωρίς το αντίστοιχο
δικαίωμα. Αφαιρέθηκαν από τον χάρτη (174 καλυμμένες ενέργειες).
**Κανόνας:** πριν προσθέσεις ενέργεια στον χάρτη, βεβαιώσου ότι δεν την καλεί οθόνη του πρώτου
(προσωπικού) group του μενού. Ο έλεγχος γίνεται αντιστοιχίζοντας `api('X')` ανά `R.<οθόνη>`.

Deep link σε οθόνη χωρίς δικαίωμα δείχνει πλέον καθαρό μήνυμα αντί για ανεπιτήρητο σφάλμα
(`go()` πιάνει το rejection του view).

**Αποστολή κωδικού.** Το `user_pass` δέχεται `send: true` και στέλνει τα στοιχεία εισόδου στο email
του χειριστή μέσω `Notify::sendTo()` — **όχι** `Notify::send()`, που σέβεται τη γενική ρύθμιση
ειδοποιήσεων και την προσωπική εξαίρεση του χρήστη· ένας κωδικός δεν είναι ειδοποίηση. Ο κωδικός
δεν καταγράφεται πουθενά: το activity log γράφει μόνο ότι στάλθηκε και πού.

**Όλα τα παράθυρα κεντρικά.** Η βάση `.drawer` έπαψε να είναι πλαϊνό συρτάρι: ανοίγει **στο κέντρο**
(860px, έως 90vh, με fade+scale) σε **όλα τα κυκλώματα** — έργα, ομάδες, προσφορές, leads, ημερολόγιο,
πρότυπα, βιογραφικά, οδηγός. Η κλάση έμεινε `.drawer` ώστε να μην αλλάξει τίποτα στη JS των **11**
σημείων που τη χτίζουν· το `.tk-modal` της εργασίας προσθέτει μόνο πλάτος (1180px) και τις δύο στήλες.
Σε κινητό όλα γίνονται πλήρης οθόνη.

**Παράθυρο εργασίας (Αύγουστος 2026).** Το πλαϊνό συρτάρι των 620px έγινε **κεντρικό modal**
(`.drawer.tk-modal`, έως 1180px × 88vh) σε δύο στήλες: αριστερά η ροή **1. Το ζητούμενο → 2. Ενέργειες
→ 3. Επικοινωνία**, δεξιά τα βοηθητικά (χρόνος, εξαρτήσεις, αρχεία). Ο διαχωρισμός γίνεται σε JS μετά
το render (`.tk-side` → `.tk-col-side`), ώστε η σειρά του DOM να μένει λογική σε κινητό.
Ο **χρόνος** και η **κατάσταση χρέωσης** ανέβηκαν στην κεφαλίδα.

**Συνομιλία:** παραλήπτης **υποχρεωτικός** (server + UI) — μήνυμα προς κανέναν δεν το διαβάζει κανείς·
**Enter** στέλνει, **Shift+Enter** νέα γραμμή (πριν έστελνε)· **συνημμένα ανά μήνυμα**
(`mod_cpm_storage` με `ref_type='comment'`, `ref_id`=id σχολίου) και οι **εικόνες φαίνονται inline**.
Το `Db::addComment()` επιστρέφει πλέον το id ώστε να κρεμαστούν τα αρχεία.

**Έγκριση λογιστηρίου:** `mod_cpm_tasks.billing_ok` / `billing_ok_by` / `billing_ok_at`. Εργασία με
χρεώσιμο χρόνο **δεν κλείνει** (409 στο `move_task`) πριν την εγκρίνει κάτοχος του κυκλώματος
**«Οικονομικά»** (`task_billing_ok`). Χωρίς αυτό ο χρόνος χανόταν σιωπηλά.

**Ανάθεση & προτεραιότητα** ξεκλείδωσαν για κάθε χειριστή που βλέπει την εργασία — ήταν κλειδωμένες
σε διαχειριστές, οπότε ο τεχνικός δεν μπορούσε να δώσει τη δουλειά σε συνάδελφο.

### Modules — δικά μας προϊόντα με checklist παράδοσης (Σεπτέμβριος 2026)

Ένα **module** είναι δικό μας προϊόν/υποδομή (PharmacyOne, e-shop, VPS setup) με τα βήματα που
χρειάζονται για να παραδοθεί σε πελάτη. Χτίστηκε **πάνω** στα πρότυπα υλοποίησης, χωρίς δεύτερο
μητρώο: `mod_cpm_templates` απέκτησε `product_id` (→ `tblproducts`, τι το πουλά) και `category`.

Ένα έργο πελάτη περιλαμβάνει **πολλά** modules: `mod_cpm_project_modules` (project ⇄ module, UNIQUE
ώστε να μην μπει δύο φορές το ίδιο) και `mod_cpm_tasks.module_id` (από ποιο module γεννήθηκε κάθε
εργασία) — γι' αυτό μέσα στο έργο το checklist ανοίγει **κάτω από το module του**.

- `project_add_modules` αντικαθιστά/γενικεύει το `template_clone` (το παλιό όνομα μένει ως alias):
  δέχεται **λίστα** modules και **είτε** νέο έργο (πελάτης + έναρξη) **είτε** υπάρχον (`project`).
  Σε υπάρχον έργο απαιτεί δικαίωμα επεξεργασίας· απορρίπτει module που υπάρχει ήδη. Η παράδοση του
  έργου προχωράει μόνο εμπρός, στο πιο μακρινό βήμα.
- `project_modules` επιστρέφει τα modules ενός έργου με τις εργασίες τους και **πρόοδο ελέγχων**
  (`checks: [ολοκληρωμένοι, σύνολο]`) ανά εργασία.
- Κεφαλίδα board: λωρίδα modules με πρόοδο· κλικ **φιλτράρει** τις κάρτες (`data-module` στην κάρτα).
- Φόρμα έργου: κάρτα «Modules της υλοποίησης» με αναδιπλούμενο checklist ανά module και προσθήκη
  module επί τόπου.
- Το μενού «Πρότυπα» μετονομάστηκε σε **Modules**.
- **Checklist παράδοσης του module** (`mod_cpm_templates.checks`, ένα ανά γραμμή): οι ενέργειες που
  πρέπει να επιβεβαιωθούν για να θεωρηθεί παραδοτέο. Στην ανάθεση γεννιέται **μία επιπλέον εργασία**
  «Παράδοση: <module>» με `is_delivery=1`, προτεραιότητα Υψηλή, ημερομηνία του τελευταίου βήματος, και
  τις ενέργειες ως `mod_cpm_checklist`.
  **Είναι απαιτητό:** το `move_task` απορρίπτει με **409** το κλείσιμο εργασίας `is_delivery` όσο μένει
  ατσέκαρη ενέργεια («Μένουν N από M ενέργειες παράδοσης»). Οι ενέργειες τσεκάρονται **επί τόπου** μέσα
  στην κάρτα του module στο έργο, χωρίς να ανοίξει η εργασία.
  Ξεχωρίζει από τους ελέγχους **των βημάτων** (`mod_cpm_template_steps.checks`), που μένουν συμβουλευτικοί.
- **Διορθώθηκε** ότι τα `.catch(() => ({ok: false}))` στο `move_task`/`move_lead` **κατάπιναν** το μήνυμα
  του server: κάθε άρνηση έβγαινε ως γενικό «Δεν επιτρέπεται» και ο χρήστης δεν μάθαινε ποτέ τον λόγο.
- **Ανάθεση από το board:** κουμπί «Ανάθεση προϊόντων» στην κεφαλίδα κάθε έργου πελάτη (εμφανίζεται
  μόνο σε όποιον μπορεί να επεξεργαστεί το έργο — το `board.meta` στέλνει `canEdit`). Ο διάλογος
  `openAssignModules()` είναι κοινός: τον καλούν και η κεφαλίδα του board και η φόρμα του έργου, ώστε
  η ανάθεση να γίνεται εκεί που δουλεύεις. Δείχνει τα ήδη ανατεθειμένα με την πρόοδό τους και μόνο
  όσα μένουν προς προσθήκη.

**Πρότυπα υλοποίησης** (Αύγουστος 2026): `mod_cpm_templates` + `mod_cpm_template_steps`.
Κάθε βήμα κρατάει τίτλο, λεπτομέρειες, **department**, προτεινόμενο υπεύθυνο, προτεραιότητα,
εκτίμηση και **ελέγχους** (ένας ανά γραμμή). Οι ημερομηνίες αποθηκεύονται ως **ημέρες από την
έναρξη** (`off_start` / `off_due` / `off_deadline`) — έτσι το ίδιο πρότυπο δουλεύει σε οποιαδήποτε
ημερομηνία. Το `template_clone` γεννά έργο πελάτη (`kind=client`, `template_id`) με μία εργασία ανά
βήμα, πραγματικές ημερομηνίες, ανάθεση + ειδοποίηση, και τους ελέγχους ως `mod_cpm_checklist`.
Η παράδοση του έργου υπολογίζεται από το τελευταίο βήμα. Οθόνη `#/templates` (Έργα → Πρότυπα).

**Ιδιωτικές σημειώσεις υπεύθυνου έργου:** `mod_cpm_projects.pm_notes`, μέσω **ξεχωριστής** ενέργειας
`project_pm_notes`. **Δεν** περνούν από το `save_project`: εκείνο χτίζει ΟΛΑ τα πεδία από το input,
οπότε μια κλήση «μόνο για τις σημειώσεις» θα μηδένιζε budget, ημερομηνίες και υπεύθυνο.
Το `portfolio` στέλνει `pmNotes` **μόνο** στον υπεύθυνο και στους διαχειριστές — για τους υπόλοιπους
το πεδίο είναι `null` και το `canPmNotes` `false`, οπότε το περιεχόμενο δεν φεύγει καν από τον server.

**Νέος πελάτης από το πεδίο επιλογής** (Αύγουστος 2026). Ο υποψήφιος συχνά δεν υπάρχει ακόμη στο
WHMCS, οπότε δεν μπορούσε να ανοίξει lead/προσφορά/έργο. Το `clientAuto()` δείχνει πλέον
**«Δημιουργία νέου πελάτη»** στο πάνελ αποτελεσμάτων· η ενέργεια `client_quick_add` καλεί
`localAPI('AddClient')` με `noemail`+`skipvalidation`. Υποχρεωτική είναι **μόνο η επωνυμία** — όσα
απαιτεί το WHMCS και δεν τα ξέρουμε (διεύθυνση, ΤΚ, τηλέφωνο) μπαίνουν ως placeholder, και όταν
λείπει email μπαίνει `lead+<τυχαίο>@noreply.cloudon.gr` **με σημείωση στην καρτέλα** ότι χρειάζεται
συμπλήρωση. Ισχύει αυτόματα και στα **έξι** σημεία που καλούν το `clientAuto` (έργα, προσφορές, CRM,
ημερολόγιο, απομακρυσμένες, vault). Το δικαίωμα είναι το κύκλωμα **«Πωλήσεις»** (ή διαχειριστής).

*Σημείωση:* τα `mod_cpm_leads` και `mod_cpm_offers` έχουν ήδη `clientid` **nullable** και δικά τους
πεδία επωνυμίας/επαφής — ένα lead μπορεί να ζήσει χωρίς καρτέλα WHMCS και να δεθεί αργότερα.

### Χρέωση διεκπεραίωσης PayPal — ακύρωση υπηρεσίας «στη λήξη περιόδου»

Όταν πελάτης ακυρώνει υπηρεσία στη λήξη της περιόδου, το WHMCS **δεν σβήνει** τη γραμμή ανανέωσης
από το ανοιχτό τιμολόγιο: της αφαιρεί `type` και `relid` και εκδίδει **πιστωτικό σημείωμα** για το
ποσό της (`Cancelled Outstanding Product Renewal Invoice Line Item`). Το σύνολο του τιμολογίου
μένει ίδιο.

Ο `cnp_surcharge_sync()` υπολόγιζε τη βάση ως `total − credit`, **αγνοώντας τα πιστωτικά σημειώματα**
του `tblaccounts`. Αποτέλεσμα: η χρέωση διεκπεραίωσης έμενε υπολογισμένη σε ποσό που δεν θα
εισπραττόταν ποτέ, και το τιμολόγιο κρατούσε **υπόλοιπο-φάντασμα** που δεν μηδενιζόταν.

Πραγματικό περιστατικό (PF20263771 / #3804, PI Marketing): δύο VPS ανανεώνονταν στις 03/09· ο
πελάτης ακύρωσε το ένα στις 31/08. Χρέωση 8,37 € υπολογισμένη στα 140,07 € αντί για **4,40 €** στα
70,67 € που πράγματι εισπράχθηκαν.

**Διόρθωση:** η βάση αφαιρεί πλέον και τις **εσωτερικές** εγγραφές του `tblaccounts` (πιστωτικά,
επιστροφές πίστωσης — όσες δεν έχουν `gateway`). Οι **πραγματικές πληρωμές πύλης δεν αφαιρούνται**:
αν μετρούσαν, η χρέωση θα μηδενιζόταν αναδρομικά μετά την πληρωμή.
Νέο hook **`UpdateInvoiceTotal`** (με φρουρό αναδρομής) ξανατρέχει τον συγχρονισμό όταν αλλάξει το
τιμολόγιο — πριν έτρεχε μόνο σε `InvoiceCreated` και `InvoiceChangeGateway`, οπότε η ακύρωση γραμμής
δεν το ενεργοποιούσε ποτέ.

**Δικαιώματα έργου** (Σεπτέμβριος 2026). Δημιουργία/επεξεργασία/αρχειοθέτηση ζητούσαν **Full
Administrator του WHMCS**, οπότε ένας project manager δεν έβλεπε καν το «Νέο project».
Νέοι βοηθοί `cnp_can_create_project()` (κύκλωμα «Έργα») και `cnp_can_edit_project()` (διαχειριστής ή
**υπεύθυνος** του έργου). Νέο έργο χωρίς δηλωμένο υπεύθυνο → υπεύθυνος όποιος το ανοίγει, αλλιώς δεν θα
μπορούσε να το ξαναγγίξει. Η διαγραφή μένει μόνο σε διαχειριστή. Το `portfolio` στέλνει `canCreate` +
ανά έργο `canEdit`· το `templates` στέλνει `canClone`. Το `project_from_offer` και το `template_clone`
ακολουθούν το ίδιο δικαίωμα δημιουργίας.

**Ανοιχτά, καταγεγραμμένα:**
1. `mod_cpm_team_depts` είναι άδειο → οι υποψήφιοι για ανάθεση βγαίνουν ακόμη από το
   `tbladmins.supportdepts` (εφεδρεία), και η κλιμάκωση σε επικεφαλής ομάδας δεν ενεργοποιείται.
2. Καμία ομάδα δεν έχει «Αναφορές & απόδοση» εκτός από τη Manager — αν οι project
   managers πρέπει να βλέπουν KPI, είναι ένα τσεκάρισμα.
3. Η ρύθμιση «Χωρίς ομάδα = καμία πρόσβαση» (`strict_areas`) είναι **κλειστή**· όποιος
   δεν ανήκει σε ομάδα παίρνει την ιστορική προεπιλογή `clients,support,projects`.
   Άναψέ τη αφού μπουν όλοι σε ομάδα.

*(Ολοκληρώθηκε 25/08/2026 — δες «Εργασία χωρίς έργο» παραπάνω.)*

**Το μενού ΕΙΝΑΙ τα δικαιώματα** (3/9/2026). Μέχρι τώρα οι ενότητες του μενού και τα
δικαιώματα ήταν δύο διαφορετικές λίστες: οι «Πελάτες» ήθελαν `support` **και** `sales`,
«Η ομάδα» ήθελε `admin` **και** `reports`, η «Διοίκηση» τρία. Ο διαχειριστής τσέκαρε ένα
κουτί και δεν μπορούσε να προβλέψει τι θα δει ο χειριστής. Τώρα **κάθε ενότητα κρατάει
ένα δικαίωμα με το ίδιο όνομα**:

| Ενότητα μενού | Δικαίωμα | Οθόνες |
|---|---|---|
| Τα δικά μου | *(κανένα)* | Η μέρα μου, Το πλάνο μου, Ο χρόνος μου, Βιβλιοθήκη, Κωδικοί, Προφίλ |
| Πελάτες | `clients` | Πελάτης 360°, CRM & leads, Προσφορές |
| Υποστήριξη | `support` | Tickets, Βάση γνώσης |
| Έργα & υλοποιήσεις | `projects` | Έργα πελατών, Board, Λίστα, Χρονοδιάγραμμα, Modules, Departments |
| Η ομάδα | *(κανένα)* | Chat, Ημερολόγιο, Standup, Απομακρυσμένες |
| Αναφορές & απόδοση | `reports` | Πλάνο ημέρας, KPI, Ανάλυση ριζών, Απόδοση χειριστών |
| Οικονομικά | `finance` | Κερδοφορία, Συμφωνία πληρωμών, Αναστολές |
| Προσλήψεις | `hr` | Βιογραφικά |
| Σύστημα | `admin` | Ομάδες & δικαιώματα, Ρυθμίσεις |

Μετονομασίες: `sales` → **`clients`** (μαζί του πήρε και τον «Πελάτη 360°», που ζούσε
στο `support`)· η ετικέτα `admin` από «Διαχείριση συστήματος» → **«Σύστημα»**. Το
`cnp_area_alias()` μεταφράζει παλιά αποθηκευμένα `sales` αν ξεφύγει γραμμή.

**Μετάπτωση δεδομένων** (μία φορά, σφραγίδα `areas_menu_v2` στο `tbladdonmodules`):
όποια ομάδα είχε `sales` **ή** `support` πήρε `clients`, για να μη χάσει κανείς την
καρτέλα πελάτη. Πρακτικά **οι Support / R&D / Accounting κέρδισαν πρόσβαση στο CRM
και στις Προσφορές** — αν δεν το θέλεις, ξετσέκαρε τους «Πελάτες» από την ομάδα τους
(Σύστημα → Ομάδες & δικαιώματα).

**«Το ένα ή το άλλο»**: ο πίνακας `cnp_action_area()` δέχεται πλέον `'α|β'` για ενέργειες
που δικαιολογημένα πατούν σε δύο ενότητες — π.χ. `client_health` (`clients|reports`,
φαίνεται και στην καρτέλα πελάτη και στο Πλάνο ημέρας), `add_expense` (`projects|finance`),
`project_from_offer` (`clients|projects`), `ticket_update` (`support|reports`),
`client_package_set` (`clients|finance`), `lead_fields` (`clients|admin`), `recurrent`
(`projects|reports`). Χωρίς αυτό, ένα ταμπλό απαιτούσε τρία δικαιώματα για να δουλέψει.

**Οι προσωπικές ενέργειες βγήκαν από τον φραγμό**: `task`, `save_task`, `move_task`,
`comment`, `time`, `time_add`, `timer_start/stop`, `check_toggle`, `watch`,
`request_update`. Η ενότητα «Τα δικά μου» δεν ζητάει δικαίωμα, άρα δεν επιτρέπεται να το
ζητάει το περιεχόμενό της — αλλιώς έβλεπες τις εργασίες σου και δεν μπορούσες να τις
ανοίξεις. Τη θέση του φραγμού την παίρνει το `Db::canSeeTask()` (ανάθεση / μπάλα / ομάδα
που εξυπηρετεί το department), που είναι **στενότερο**. Το `check_toggle` και το `watch`
δεν είχαν κανέναν δικό τους έλεγχο — **τους μπήκε** `canSeeTask`. Το `save_task` δεν
δημιουργεί εργασία (απαιτεί υπαρκτή), γι' αυτό είναι ασφαλές· το `quick_task` δημιουργεί
και έμεινε στο `projects`.

**34 διπλοί έλεγχοι `if (!$FULL)` αφαιρέθηκαν** από ενέργειες που έχουν πλέον δικαίωμα
(ίδιο λάθος με τα KPI/απόδοση/πλάνο ημέρας παλιότερα): `settings_get/save`, `teams`,
`save_team`, `users`, `autos`, `tcats`, `canned_*`, `status_*`, `type_*`, `tquota*`,
`recurring*`, `targets*`, `profit`, `kb_del`, `request_update` κ.ά. Χωρίς αυτό, το
δικαίωμα «Σύστημα» δεν άνοιγε τίποτα. **Παραμένουν μόνο για Full Administrator** οι
ενέργειες που πιάνουν λογαριασμό WHMCS: `user_save`, `user_pass`, `user_toggle`,
`user_del`, `addon_access_grant` — το «Σύστημα» δίνει τις ρυθμίσεις της εφαρμογής, όχι
τη δημιουργία διαχειριστή WHMCS.

**Γλώσσα**: όλα τα μηνύματα λένε πλέον «ενότητα», όχι «κύκλωμα», και ονομάζουν αυτήν που
λείπει: *«Δεν έχεις πρόσβαση στην ενότητα «Αναφορές & απόδοση» — ζήτησέ το από τον
διαχειριστή»*. Έξι οθόνες που έδειχναν γενικό μήνυμα (Πλάνο ημέρας, Ανάλυση ριζών,
Στόχοι, Κερδοφορία, Αναφορές CRM, Βιογραφικά) περνούν πλέον από το `cnpDenied()` και
δείχνουν το πραγματικό. Η φράση «Χρειάζεσαι ειδικότητα «HR»» έφυγε.

**Έλεγχος**: 32 οθόνες × 2 ρόλους χωρίς σφάλμα JS· διασταύρωση 102 κλήσεων API με το
δικαίωμα της ενότητάς τους (μηδέν ασυμφωνίες)· ζωντανός έλεγχος 27 actions με λογαριασμό
`mkontos` (clients+support+projects) — περνούν όσα πρέπει, κόβονται όσα πρέπει.

**«Το πλάνο μου» έγινε to-do list** (3/9/2026). Ήταν λίστα **ανά έργο**: για να γράψεις
μία αράδα έπρεπε πρώτα να βρεις καρτέλα έργου, και πάνω σε κάθε κάρτα καθόταν ένα
textarea «πού έμεινα» που έπνιγε τη λίστα. Αντιστράφηκε: **το έργο είναι ετικέτα, όχι
φάκελος**, και η σειρά βγαίνει από τον χρόνο.

- Μία γραμμή γραψίματος πάνω-πάνω (Enter, ο κέρσορας μένει εκεί), με προαιρετικό έργο
  και ημερομηνία δίπλα.
- Αυτόματοι κάδοι: Εκπρόθεσμα / Σήμερα / Αύριο / Μέσα στην εβδομάδα / Αργότερα /
  Χωρίς ημερομηνία / Ολοκληρωμένα (κλειστά).
- Πάνω στη γραμμή: τικ, **επεξεργασία κειμένου επί τόπου** (Enter/Esc), αλλαγή έργου από
  την ετικέτα, μενού ημερομηνίας με γρήγορες επιλογές (αντικατέστησε το `cnpPrompt` που
  ζητούσε να πληκτρολογήσεις «ΕΕΕΕ-ΜΜ-ΗΗ ΩΩ:ΛΛ»), σύρσιμο για σειρά.
- Οι σημειώσεις «πού έμεινα» **δεν χάθηκαν** — μαζεύτηκαν σε ένα πτυσσόμενο τμήμα, μία
  ανά έργο, κλικ για επεξεργασία.
- Δεύτερη προβολή «Κατά έργο» (η παλιά λογική) με διακόπτη· η επιλογή θυμάται.
- API: το `todos_list` επιστρέφει και επίπεδη `items`/`notes`/`tasks`/`projects`
  (τα `groups` μένουν για την προβολή ανά έργο)· το `todo_update` δέχεται `project`
  (μετακίνηση)· το `todo_clear_done` δέχεται `all`· το `todo_seed` δέχεται λίστα
  `tasks` και παραλείπει ό,τι υπάρχει ήδη, οπότε πατιέται ξανά χωρίς διπλότυπα.

**Διόρθωση: το `--info` δεν οριζόταν πουθενά.** Κώδικας έγραφε `var(--info)` (χρώμα
«καλυμμένο από προσφορά») χωρίς να υπάρχει η μεταβλητή — το χρώμα έπεφτε σιωπηλά στο
κληρονομούμενο. Ορίστηκε στο `app.css` και στα δύο θέματα (`#0374b0` / `#4db3ea`,
ίδιο με το `.pill-info`).

**Προαγορά χρόνου — κάλυψη χρεώσιμης δουλειάς** (3/9/2026). Το module
`supportcontracts` υπήρχε από τον Ιούλιο με τράπεζα ωρών ανά πελάτη, ledger κινήσεων,
3 προϊόντα WHMCS που πιστώνουν αυτόματα με το `InvoicePaid`, και γέφυρα από το PM
(`Time::push`) που έσπρωχνε κάθε καταχώρηση χρόνου στο worklog. **Ήταν όμως νεκρό**:
κανένας πελάτης δεν είχε συμβόλαιο, το cron της εβδομαδιαίας αναφοράς δεν ήταν
προγραμματισμένο πουθενά, και τίποτα δεν φαινόταν μέσα στο PM.

**Δεν φτιάχτηκε δεύτερο μητρώο** — το μητρώο μένει στους πίνακες
`mod_supportcontracts_*`. Προστέθηκε από πάνω η έννοια της **κάλυψης**: κάθε χρεώσιμο
λεπτό προέρχεται από μία πηγή, και ξέρουμε ποια.

- `lib/Cover.php` — η μηχανή. Σειρά: **εγκεκριμένη προσφορά του έργου** (πουλήθηκε
  γι' αυτή τη δουλειά) → **προαγορά** → **ακάλυπτο**. Μερική άντληση: αν μένουν 10΄ και
  η χρέωση είναι 30΄, παίρνει τα 10΄ και τα 20΄ μένουν ακάλυπτα, ώστε να μη μένουν
  ορφανά λεπτά σε καμία πηγή.
- **Το υπόλοιπο δεν πάει ποτέ αρνητικό.** Αρνητικό υπόλοιπο κρύβει το τι πρέπει να
  τιμολογηθεί· ο ακάλυπτος χρόνος είναι λίστα που την κοιτάς και ενεργείς. (Το παλιό
  `Time::push` βυθιζόταν σε αρνητικό — άλλαξε.)
- Νέες στήλες: `mod_cpm_timelogs.cover` / `cover_offer_id` / `cover_minutes`,
  `mod_cpm_offers.covered_minutes`, `mod_supportcontracts_clients.report_freq`.
- `check_toggle` και `watch` δεν είχαν κανέναν έλεγχο — τους μπήκε `canSeeTask`
  (βγήκαν από τον φραγμό περιοχής μαζί με τις άλλες προσωπικές ενέργειες).

**Οι δύο τρόποι δουλειάς που ζητήθηκαν** και οι δύο υποστηρίζονται: *πρώτα η
κοστολόγηση* (προσφορά → αποδοχή → σύνδεση με έργο → ο χρόνος τραβιέται από εκεί) και
*πρώτα η δουλειά* (γράφεις χρόνο, και από τα ακάλυπτα βγάζεις προσφορά). Η δημιουργία
προσφοράς **δεσμεύει** τις καταχωρήσεις (`cover_offer_id`) ώστε ο ίδιος χρόνος να μην
μπει σε δεύτερη· αν η προσφορά γίνει `lost`, ελευθερώνονται (hook στο `move_offer`).
Χρόνος σε προσφορά που δεν έχει γίνει ακόμη δεκτή μετριέται χωριστά (`pending`) —
δεν είναι ούτε ακάλυπτος ούτε εξασφαλισμένος.

**Νέα ενότητα «Προαγορά χρόνου»** με δικαίωμα `prepaid` (όγδοο, ίδιος κανόνας:
μία ενότητα = ένα δικαίωμα). Δύο οθόνες σε νέο `views6.js`: *Υπόλοιπα πελατών*
(συμβόλαιο, πίστωση/διόρθωση, ανάλυση περιόδου ανά έργο, κινήσεις, ακάλυπτα →
προσφορά) και *Ακάλυπτος χρόνος* (τι πρέπει να τιμολογηθεί, ανά πελάτη).

**Αναφορά προς τον πελάτη** — `lib/Report.php`, μία πηγή για προεπισκόπηση, χειροκίνητη
αποστολή και cron. Ομαδοποίηση **ανά έργο/υπηρεσία** με τις εργασίες από κάτω, τι
πληρώθηκε από προαγορά, τι από προσφορά, τι έμεινε εκτός, και το διαθέσιμο υπόλοιπο.
Επεξεργάσιμο template WHMCS «Support Contract Statement». Cron
`cloudonprojects/crons/prepaid_report.php`:

```
0 8 1 * *   … prepaid_report.php monthly    # την 1η, για τον μήνα που έκλεισε
0 18 * * 5  … prepaid_report.php weekly     # Παρασκευή 18:00
```

Η συχνότητα ορίζεται **ανά πελάτη** στο συμβόλαιο (μηνιαία / εβδομαδιαία / και τα δύο /
καμία). Κενή αναφορά δεν στέλνεται ποτέ. Δοκιμή χωρίς αποστολή: `CPM_DRY=1 php … monthly`.
Το παλιό `supportcontracts/crons/weekly_report.php` **αντικαταστάθηκε** — σημειώθηκε στο
αρχείο και η ρύθμιση `weekly_report` μπήκε `off`, ώστε να μη σταλεί δεύτερο email αν
κάποιος το ξαναβάλει στο cron.

**Ανοιχτά:**
1. **Κανένας πελάτης δεν έχει ακόμη συμβόλαιο** — μέχρι τότε όλα τα παραπάνω είναι
   αδρανή: ο χρόνος καταγράφεται και εμφανίζεται ως ακάλυπτος, κανένα email δεν φεύγει.
2. Καμία ομάδα δεν έχει το δικαίωμα «Προαγορά χρόνου» — μόνο οι full admins
   το βλέπουν.
3. Η τιμή πώλησης ώρας (`hour_rate`) αποθηκεύεται την πρώτη φορά που φτιάχνεις
   προσφορά από ακάλυπτο χρόνο· δεν έχει οριστεί ακόμη.
4. Τα 3 προϊόντα «Προαγορά Υποστήριξης 10ω/20ω/50ω» δεν τα έχει αγοράσει κανείς.

**Οθόνη «Δραστηριότητα»** (3/9/2026), μέσα στις «Αναφορές & απόδοση» (δικαίωμα `reports`,
χωρίς νέο δικαίωμα). Απαντά σε δύο ερωτήσεις: ποιος δουλεύει τώρα και σε τι, και τι έγινε.

- **Η ομάδα τώρα**: κάρτα ανά άτομο με τρέχον χρονόμετρο (εργασία, έργο, πόση ώρα),
  ανοιχτή απομακρυσμένη σύνδεση, ή την τελευταία του κίνηση. Δίπλα: ανοιχτές εργασίες,
  πόσες περιμένουν ενέργειά του, tickets, χρόνος σήμερα, τι έκλεισε. Πρώτοι όσοι δουλεύουν.
- **Ροή** από τρεις πηγές μαζί: `mod_cpm_activity`, **απαντήσεις σε tickets**
  (`tblticketreplies` — εκεί ζει η μισή δουλειά και δεν φαινόταν πουθενά αλλού) και
  καταχωρήσεις χρόνου. Ομαδοποίηση ανά ημέρα, φίλτρο ανά άτομο, διάστημα 8ω/24ω/3 μέρες.
- Αυτόματη ανανέωση κάθε 30" — παγώνει όταν η καρτέλα δεν είναι ορατή και σβήνει όταν
  αλλάξεις οθόνη.

**Διόρθωση: η παρουσία ήταν ψεύτικη.** Το `last_seen` το ενημέρωναν ΜΟΝΟ οι ενέργειες του
chat (`chat_channels`/`chat_msgs`/`chat_send`), οπότε όποιος δούλευε στο board χωρίς να
ανοίξει chat έδειχνε «απών» — και το «Online» της πάνω μπάρας έλεγε ψέματα. Μπήκε στο
`version` (ο σφυγμός κάθε 12" που καλεί κάθε ανοιχτή εφαρμογή), με γράψιμο μόνο αν έχει
περάσει μισό λεπτό.

Νέος βοηθός `cnp_admin_by_name()`: οι απαντήσεις σε tickets κρατούν ΟΝΟΜΑ, όχι id — η
λογική χαρτογράφησης ήταν θαμμένη μέσα στο `perf`, τώρα είναι κοινή. Λογαριασμοί
bot/system μένουν έξω από τη λίστα της ομάδας.


**Κλειστό μενού: όνομα στο hover και κύλιση** (3/9/2026). Με 34 εικονίδια σε οθόνη
λάπτοπ (760px) το μαζεμένο μενού έπιανε **1528px**: 635px έμεναν έξω από την οθόνη και
το υποσέλιδο (προφίλ, θέμα, αποσύνδεση) ήταν **εντελώς άφταστο**.

- Οι ενότητες μπήκαν σε `<nav class="snav">` με `overflow-y:auto`. Η μπάρα κύλισης είναι
  **κρυφή** (`scrollbar-width:none` + `::-webkit-scrollbar{display:none}`) — η στήλη είναι
  στενή και η μπάρα καθόταν πάνω στα εικονίδια· η κύλιση δουλεύει με ρόδα και αφή.
  Το `.side-foot` έγινε `flex:none`, οπότε μένει πάντα κάτω. Ωφελείται και το **ανοιχτό**
  μενού: με όλες τις ενότητες ανοιχτές έπιανε 1666px σε 542px διαθέσιμα.
- Το tooltip έπαψε να είναι `::after` και έγινε πραγματικό στοιχείο στο `<body>`
  (`sideTipShow`): μέσα σε δοχείο που κυλάει, ένα ψευδοστοιχείο στο +58px θα κοβόταν
  οριζόντια. Δείχνει **όνομα οθόνης + ενότητα** (`data-grp`), γιατί μαζεμένο οι
  επικεφαλίδες δεν φαίνονται πουθενά.
- Στην κύλιση το tooltip **ακολουθεί** το εικονίδιο αντί να κρύβεται — αλλιώς όποιος
  κυλάει με τη ρόδα χωρίς να κουνήσει τον δείκτη έμενε χωρίς όνομα. Κρύβεται μόνο όταν
  το εικονίδιο βγει από το ορατό μέρος της στήλης.
- Λεπτή γραμμή ανάμεσα στις ενότητες όταν είναι μαζεμένο, ώστε η ομαδοποίηση να μη χάνεται.
- Ένας ακροατής `mouseover` στη στήλη αντί για 34 ακροατές που ξαναδένονται σε κάθε render.

Έλεγχος: 34/34 εικονίδια δείχνουν tooltip εντός οθόνης σε ύψη 760px και 600px· το
υποσέλιδο ορατό και στα δύο· στο κινητό δεν εμφανίζεται tooltip και η κάτω μπάρα μένει ίδια.


**Τα δύο εγχειρίδια** (4/9/2026).

- **`TECHNICAL.md`** — νέο. Αρχιτεκτονική, σημεία εισόδου, το μοντέλο δεδομένων (77
  πίνακες κατά ομάδα), το σύστημα δικαιωμάτων, η αλυσίδα χρόνου→κάλυψης→χρέωσης, οι
  ενσωματώσεις, το cron, **οι παγίδες που έχουν ήδη κοστίσει**, και οδηγίες «πώς
  προσθέτεις οθόνη / δυνατότητα / πίνακα». Το `UPGRADE.md` παραμένει το ιστορικό
  αλλαγών και η λίστα ελέγχου της αναβάθμισης· δείχνει πλέον στο νέο.
- **Οδηγός χρήσης** (`help.js`) — από 28 σε **39 κεφάλαια**. Έλειπαν έντεκα οθόνες:
  Το προφίλ μου, Modules, Departments, Πλάνο ημέρας, KPI, Ανάλυση ριζών, Απόδοση
  χειριστών, Κερδοφορία, Συμφωνία πληρωμών, Αναστολές, Ρυθμίσεις. Η σειρά **ακολουθεί
  πλέον το μενού** και η αρίθμηση ξανάγινε καθαρή 1–39 (είχε φτάσει σε «21β/21γ/21δ/21ε»).
  Το παλιό κεφάλαιο «Διοίκηση» αφαιρέθηκε: ήταν περίληψη ενός κυκλώματος που δεν
  υπάρχει πια, και κάθε στοιχείο του έχει τώρα δικό του κεφάλαιο.
  Το `VIEW_TO_HELP` ξαναχτίστηκε — **44 αντιστοιχίσεις, καμία σπασμένη, καμία οθόνη
  χωρίς βοήθεια**.

**31 φραγμοί που ακύρωναν δικαιώματα** (4/9/2026). Η σάρωση για την τεκμηρίωση
αποκάλυψε 31 ενέργειες με σκέτο `if (!$FULL)` **πάνω από τη δυνατότητά τους**: Modules,
διαγραφή έργου, Automations, departments WHMCS, αναστολές, συμφωνία πληρωμών,
λογιστικός έλεγχος, εισαγωγή/εξαγωγή leads. Έδινες το δικαίωμα και δεν άνοιγε τίποτα.
Οι προηγούμενες σαρώσεις έψαχναν την τρίγραμμη μορφή· αυτές ήταν μονόγραμμες.
Μένουν πέντε, όλοι για ενέργειες σε λογαριασμό WHMCS. Οι έλεγχοι **ιδιοκτησίας**
(«full ή ο ίδιος») δεν πειράχτηκαν.

**Διόρθωση προηγούμενης δήλωσης.** Είχα γράψει ότι «το cron της εβδομαδιαίας αναφοράς
δεν ήταν προγραμματισμένο πουθενά». **Λάθος** — ήταν στο `/etc/cron.d/supportcontracts-weekly`,
και είχα κοιτάξει μόνο το crontab του root. Θα έτρεχε Παρασκευή 18:00, την ίδια ώρα με το
νέο. Τα διπλά email τα απέτρεψε η ρύθμιση `weekly_report=off` που είχε μπει. Η εγγραφή
cron αφαιρέθηκε.

**Παράπονα πελατών** (4/9/2026). Νέα οθόνη «Πελάτες → Παράπονα», δύο δυνατότητες:
`clients.complaints` (καταγραφή + προβολή — την έχει όποιος μιλάει με πελάτες) και
`clients.complaint_close` (κλείσιμο με έκβαση — το έχει όποιος λογοδοτεί). Ο supporter
καταγράφει ελεύθερα χωρίς να μπορεί να κλείσει μόνος του ό,τι κατέγραψε.

**Γιατί δικό του μητρώο.** Το παράπονο δεν είναι ticket (αίτημα) ούτε επικοινωνία
(καταγραφή επαφής): είναι δυσαρέσκεια με κύκλο ζωής (ανοιχτό → σε χειρισμό → λύθηκε /
αβάσιμο) και απολογισμό. Νέοι πίνακες `mod_cpm_complaints` και
`mod_cpm_complaint_notes` (το ιστορικό χειρισμού), με συνδέσεις προς ticket, εργασία,
έργο και επικοινωνία.

- **Καταχώρηση σε δεκαπέντε δευτερόλεπτα**: πελάτης (αναζήτηση με όνομα ή τηλέφωνο,
  ίδια μηχανή με την κλήση), το άτομο που μίλησε, το παράπονο **με τα λόγια του**,
  κατηγορία, σοβαρότητα, πηγή, υπεύθυνος.
- **Κρίσιμο παράπονο ειδοποιεί αμέσως** όσους έχουν `clients.complaint_close` — δεν
  περιμένει να το δει κάποιος στη λίστα.
- **Δεν κλείνει χωρίς έκβαση** γραμμένη, και ζητά και **αιτία** σε δυο λέξεις: αυτή
  φτιάχνει το μοτίβο. Το «αβάσιμο» θέλει κι αυτό εξήγηση — τη διαβάζει όποιος το
  κατέγραψε, που ειδοποιείται στο κλείσιμο.
- **«Γυρίσαμε στον πελάτη»**: χωριστό κουτάκι. Παράπονο που λύθηκε χωρίς να το μάθει
  ο πελάτης, γι' αυτόν δεν λύθηκε.
- **Τι επαναλαμβάνεται**: μπάρα κατηγοριών εξαμήνου (κλικ = φίλτρο), πελάτες με πάνω
  από ένα παράπονο, μέσος χρόνος επίλυσης 90 ημερών. Οι κατηγορίες είναι **σταθερή
  λίστα** επίτηδες: αλλιώς δεν συγκρίνεις μήνες.

**Ενοποίηση**: ο κανόνας «είναι άνθρωπος ή ρομπότ;» ήταν γραμμένος τρεις φορές αλλιώς
(χαρτογράφηση ονόματος, Δραστηριότητα, ειδοποιήσεις). Έγινε `cnp_is_bot()` — οι
ειδοποιήσεις κρίσιμου παραπόνου πήγαιναν και σε «CloudOn Support Team Team».


**Καταγραφή κλήσης** (4/9/2026). Νέα δυνατότητα `clients.calls`. Χτύπησε το τηλέφωνο,
το έκλεισες: σε δεκαπέντε δευτερόλεπτα μένει γραπτό ποιος πήρε, τι ζήτησε και **τι
γίνεται με αυτό** — μια κλήση χωρίς συνέχεια είναι σημείωση που θα χαθεί.

- Ανοίγει με **`v`** από παντού, ή «+ Νέο → Καταγραφή κλήσης» (το παλιό «Νέα επικοινωνία»
  απλώς σε πήγαινε σε άλλη οθόνη).
- **Ποιος πήρε**: αναζήτηση σε πελάτες ΚΑΙ leads, με όνομα ή **αριθμό**. Τα τηλέφωνα του
  WHMCS είναι μορφοποιημένα («+30.21 5215 1661»), οπότε η σύγκριση γίνεται σε σκέτα
  ψηφία (`cnp_phone_norm` + `REPLACE` στο SQL) στα τελευταία 9. Άγνωστος καλών
  καταγράφεται κανονικά στο `caller`/`phone`.
- **Και μετά;** μόνο καταγραφή · **εργασία** (task «☎ …» με τον αριθμό στην περιγραφή,
  έργο και ανάθεση) · **ticket** (μέσω `OpenTicket`, χρεωμένο στον καταγράφοντα).
  Καθένα ζητά τη δική του δυνατότητα (`projects.board` / `support.tickets`) και το κουμπί
  κρύβεται αν λείπει. Προαιρετική υπενθύμιση με ημερομηνία.
- **Ο χρόνος της κλήσης** (5/10/15/30΄ ή ελεύθερα) περνάει από την ίδια μηχανή κάλυψης:
  αφαιρείται από την προαγορά ή μένει ακάλυπτος.

**Διόρθωση στη γέφυρα χρόνου.** Το `Time::push()` έβρισκε τον πελάτη ΜΟΝΟ από το έργο ή
το ticket της εργασίας. Μια εργασία γεννημένη από τηλεφώνημα δεν έχει ούτε τα δύο, οπότε
ο χρεώσιμος χρόνος έμενε **αχρέωτος** σαν εσωτερική δουλειά — `charged_minutes = 0`.
Πήρε δεύτερη παράμετρο `$clientHint`: τον πελάτη που ξέρει ο καλών. Επαληθεύτηκε
ζωντανά (120΄ προαγορά → 105΄ μετά από 15λεπτη χρεώσιμη κλήση).

Νέες στήλες στον `mod_cpm_interactions`: `phone`, `caller`, `direction` (in/out),
`minutes`, `task_id`, `ticketid`. **Κανένα νέο μητρώο** — οι κλήσεις ζουν εκεί που ζουν
όλες οι επικοινωνίες και φαίνονται στο ιστορικό πελάτη/lead.

Η δυνατότητα δίνεται **χωριστά από το CRM**: ένας τεχνικός καταγράφει κλήσεις χωρίς να
βλέπει το funnel πωλήσεων.


**Δικαιώματα ανά δυνατότητα, όχι ανά ενότητα** (4/9/2026). Το μοντέλο ήταν «όλη η
ενότητα ή τίποτα»: για να δώσεις τις Αναστολές έδινες και την Κερδοφορία, για το Board
έδινες και τη διαγραφή έργου. Τώρα κάθε ενότητα σπάει σε **δυνατότητες** — μία ανά οθόνη,
συν όσες ενέργειες έχουν σοβαρή συνέπεια. **Το μοντέλο μένει ίδιο**: ομάδα κρατά
δικαιώματα, χρήστες μπαίνουν στην ομάδα. Δεν προστέθηκε τρίτο επίπεδο «ρόλου».

- **39 δυνατότητες** σε 8 ενότητες (`cnp_caps()`): 23 οθόνες + 16 ενέργειες. Κλειδί
  `ενότητα.δυνατότητα`. Παραδείγματα: `projects.delete`, `finance.suspend_do`,
  `support.kb_edit`, `prepaid.move`, `admin.users`.
- **Πλήρης συμβατότητα**: το αποθηκευμένο `projects` σημαίνει «όλη η ενότητα» και
  καλύπτει κάθε δυνατότητά της — **και ό,τι προστεθεί αύριο**. Καμία υπάρχουσα ομάδα δεν
  χρειάστηκε μετάπτωση. Όταν τσεκάρεις όλες τις δυνατότητες μιας ενότητας,
  το `cnp_perm_clean()` τις **μαζεύει πίσω** στο κλειδί της ενότητας, ώστε η ομάδα να μη
  μείνει πίσω σε επόμενη προσθήκη.
- Ο πίνακας `cnp_action_area()` έγινε `cnp_action_cap()`: **187 ενέργειες**, καμία δεν
  έχασε φραγμό (διασταυρώθηκε με τον παλιό πίνακα). Το «α|β» σημαίνει «φτάνει η μία».
- `mod_cpm_teams.areas` και `mod_cpm_prefs.value` έγιναν **TEXT** — 160 χαρακτήρες δεν
  χωρούσαν 39 κλειδιά και η λίστα θα κοβόταν σιωπηλά.

**Η καρτέλα της ομάδας**: δέντρο ενότητα → δυνατότητες. Το κουτάκι της ενότητας είναι
τριπλής κατάστασης (όλα / μερικά / κανένα) με μετρητή «3/8». Οι ενέργειες ξεχωρίζουν με
μοβ ετικέτα από τις οθόνες (μπλε). Αν τσεκάρεις ενέργεια που προϋποθέτει οθόνη
(`needs` στο μητρώο), τσεκάρεται και η οθόνη και αναβοσβήνει. Η κάρτα της ομάδας έγινε
**σύνοψη** — τα inline κουτάκια που αποθήκευαν άμεσα δεν στέκουν με 39 δυνατότητες.

**Πρότυπα** (`mod_cpm_perm_presets`): ονομασμένα πακέτα που γεμίζουν τα κουτάκια —
Υποστήριξη, Τεχνικός, Project Manager, Λογιστήριο, Πωλήσεις, Διαχειριστής συστήματος.
**Αφετηρία μόνο**: τα δικαιώματα ζουν πάνω στην ομάδα, οπότε αλλαγή προτύπου δεν αλλάζει
καμία υπάρχουσα ομάδα — κανένα κρυφό δεύτερο μητρώο.

**Η διεπαφή κρύβει ό,τι δεν μπορείς να πατήσεις**, αλλιώς το λεπτομερές δικαίωμα θα
σήμαινε απλώς κουμπιά που πέφτουν σε 403: εκτέλεση αναστολής, επεξεργασία βάσης γνώσης,
διαγραφή έργου, επαναλαμβανόμενες, ενέργειες προαγοράς, καρτέλες Ρυθμίσεων
(Automations / Χρήστες / Departments WHMCS), καρτέλα Θέσεων. Κοινός βοηθός `cnpCan()` σε
όλα τα views.

**24 ακόμη διπλοί έλεγχοι αφαιρέθηκαν** (17 στις Προσλήψεις, 3 σε HR/αρχεία, 4 αλλού) που
ζητούσαν ΟΛΗ την ενότητα και ακύρωναν το σπάσιμο — ίδιο μοτίβο με τα KPI παλιότερα. Το
`cv_jobs` και το `kb_use` έγιναν «α|β», γιατί τα διαβάζουν και γειτονικές οθόνες.
Η **έγκριση χρέωσης** μετακινήθηκε από τα Έργα στα Οικονομικά (`finance.billing_ok`) —
εκεί ανήκει, όπως έλεγε ήδη το σχόλιο του κώδικα. Το `portfolio` στέλνει πλέον
`canDelete`/`canShare`/`canRecur` αντί για σκέτο `canManage = $FULL`.

**Μόνο για Full Administrator** μένουν οι ενέργειες σε λογαριασμό WHMCS: `user_save`,
`user_pass`, `user_toggle`, `user_del`, `addon_access_grant`. Το «Σύστημα» δίνει τις
ρυθμίσεις της εφαρμογής, όχι τη δημιουργία διαχειριστή WHMCS.

**Έλεγχος**: 187/187 ενέργειες χαρτογραφημένες, κάθε δυνατότητα του μητρώου έχει
ενέργειες και αντίστροφα· ζωντανή δοκιμή με λογαριασμό που είχε «οθόνες χωρίς τις
ενέργειές τους» — και τα 6 σύνολα κουμπιών κρύφτηκαν, μηδέν 403· 35 οθόνες × 2 ρόλους
χωρίς σφάλμα JS.

**Ομοιόμορφες επικεφαλίδες μενού** (3/9/2026). Το `<button>` κεντράρει το κείμενο από
προεπιλογή, οπότε μια επικεφαλίδα που δεν χωρούσε έσπαγε στα δύο και έδειχνε
στοιχισμένη στο κέντρο — έσπαγε την ομοιομορφία της στήλης. Ο τίτλος μπήκε σε δικό του
`.sgroup-t` (μία γραμμή, `text-overflow:ellipsis`), το κουμπί πήρε `text-align:left`, και
το αραίωμα γραμμάτων έπεσε 1px → .55px ώστε να χωρά ο μακρύτερος τίτλος («Αναφορές &
απόδοση», 173px σε 168px διαθέσιμα). Το «Προαγορασμένος χρόνος» μετονομάστηκε σε
**«Προαγορά χρόνου»** — και στο μενού και στο δικαίωμα, ώστε να μείνει ο κανόνας
«ίδιο όνομα και στα δύο». Και οι 11 επικεφαλίδες: ίδιο ύψος 34px, ίδια στοίχιση 21px.


**Κοστολόγηση PharmacyOne μέσα στις Προσφορές** (4/9/2026). Η κοστολόγηση φαρμακείου
ζούσε σε αυτόνομο αρχείο HTML με τους τύπους σε JavaScript: έβγαζε τιμή, τύπωνε PDF, και
από εκεί και πέρα η προσφορά **έβγαινε από το κύκλωμα** — κανείς δεν ήξερε αν στάλθηκε,
αν απάντησαν, αν χάθηκε. Τώρα ο ίδιος υπολογισμός γεννά **κανονική προσφορά** με στάδια,
`sent_at`/`replied_at`, follow-up και Quote, όπως κάθε άλλη.

- **`lib/Pharmacy.php`** — ο κατάλογος και οι τύποι στον server: 17 παράμετροι, 19 modules
  σε 3 ομάδες, 32 γραμμές τιμοκαταλόγου, 4 εκδόσεις, 12 χαρακτηριστικά, 5 ομάδες κόστους.
  Η μεταφορά **επαληθεύτηκε αριθμό προς αριθμό**: οι αρχικοί τύποι JS εξήχθησαν από το
  φύλλο και τρέξαμε 5 σενάρια παραμέτρων και στα δύο — **820 τιμές, ακριβής ταύτιση**.
  Διατηρήθηκε και η ιδιομορφία της γραμμής 38 του φύλλου (`L4` για την πρώτη έκδοση,
  `K8` για τις υπόλοιπες) — είναι η συμπεριφορά που τιμολογούσαμε μέχρι σήμερα.
- **Ένα σημείο αλήθειας για το ποσό.** Ο υπολογισμός γίνεται **μόνο** στον server· η οθόνη
  ρωτάει (`pharmacy_calc`) και δείχνει. Έτσι το ποσό της προσφοράς, τα σύνολα των καρτών
  και οι αριθμοί του εγγράφου δεν γίνεται να αποκλίνουν.
- **Καμία νέα δομή προσφοράς.** Ο `mod_cpm_offers` πήρε δύο στήλες: `kind`
  (`plain` | `pharmacyone`, προεπιλογή `plain` — και οι 19 υπάρχουσες προσφορές έμειναν
  `plain`) και `config` (TEXT, η ρύθμιση της κοστολόγησης). Η προσφορά μπαίνει σε στάδιο
  **Πρόχειρη**, με ποσό το πρώτο έτος μείον έκπτωση προ ΦΠΑ, ανατεθειμένη στον δημιουργό
  και με `expected_close` όσο η ισχύς που δηλώθηκε.
- **Ενέργειες**: `pharmacy_defs`, `pharmacy_calc`, `pharmacy_save`, `pharmacy_doc` — όλες
  κάτω από το **υπάρχον** δικαίωμα `clients.offers`. Δεν προστέθηκε δυνατότητα: είναι
  τρόπος να φτιάξεις προσφορά, όχι χωριστό κύκλωμα.
- **Οθόνη** (`views7.js`, νέο): κουμπί «PharmacyOne» δίπλα στο «Νέα προσφορά», τέσσερις
  καρτέλες (Παράμετροι / Modules / Τιμοκατάλογος / Έγγραφο), οι 4 εκδόσεις πάντα
  δίπλα-δίπλα για σύγκριση, ζωντανά σύνολα με debounce 220ms. Τα modules είναι
  **διακόπτες**, όχι κουτάκια επιλογής (το υπάρχον `.switch` του πίνακα): εκεί δηλώνεται
  τι περιλαμβάνει η προσφορά, και το «μέσα / έξω» πρέπει να διαβάζεται με μια ματιά.
  Κάθε ομάδα δείχνει μετρητή «3/9». Το έγγραφο (7 σελίδες)
  προβάλλεται σε **iframe**: το στυλ του δεν συγκρούεται με του πίνακα, δεν αλλάζει με το
  θέμα, και τυπώνεται ακριβώς όπως φαίνεται — χωρίς popup που μπλοκάρει ο browser.
- **ΑΦΜ**: το κουμπί «Άντληση ΑΑΔΕ» χτυπά το **ίδιο** `afm.php` που τροφοδοτεί τη φόρμα
  εγγραφής (RgWsPublic2, cache 30 ημερών). Δεν χρειάστηκε ο PowerShell proxy — η κλήση
  γινόταν ήδη server-side, και τα διαπιστευτήρια δεν φεύγουν ποτέ προς τον browser.
- Το άνοιγμα προσφοράς με `kind = 'pharmacyone'` **δρομολογείται στον κοστολογητή**, όχι
  στη γενική φόρμα· αλλιώς η επόμενη αποθήκευση θα έσβηνε το υπολογισμένο ποσό.
- **Το έντυπο** (Σεπτέμβριος 2026) πήρε τη μορφή που στέλνουμε χρόνια στους πελάτες:
  δεκατρείς σελίδες Α4 — εξώφυλλο με τα δύο λογότυπα και τα μπλε/κόκκινα πλαίσια,
  συνοδευτική επιστολή, περιεχόμενα με στικτούς οδηγούς, εισαγωγή, Soft1 Open Enterprise
  Edition, ο πίνακας «Soft1 ERP → Modules included» με τις μπλε τελείες (ακολουθεί την
  έκδοση: Β → Έσοδα-Έξοδα, ΒΓ → Γενική Λογιστική, Plus → Παρτίδες / Business Units /
  Τιμολογιακές Πολιτικές), οι πίνακες κόστους ανά ομάδα, οι συγκεντρωτικοί με την κόκκινη
  γραμμή έκπτωσης, ο τρόπος πληρωμής με τους λογαριασμούς, και το Έντυπο Αποδοχής.
  Το στυλ μετακόμισε στον server (`Pharmacy::docCss`) ώστε προεπισκόπηση και εκτύπωση —
  και αύριο το email — να διαβάζουν το ίδιο κείμενο.
- **Ο χαιρετισμός της επιστολής έγινε πεδίο** («Αξιότιμοι κύριοι,» ως αφετηρία). Το φύλο
  δεν συμπεραίνεται από το όνομα· το γράφει ο πωλητής.

**Σύγχρονη σχεδίαση του εντύπου** (4/9/2026). Ίδιο περιεχόμενο, νέα όψη: το έντυπο έδειχνε
Word του 2005 — γκρι μπάντες, στικτοί οδηγοί, βαριά περιγράμματα.

- **Παλέτα**: βαθύ navy για τα κείμενα, το μπλε της CloudOn ως μοναδική έμφαση, ουδέτερα με
  ελαφρά μπλε απόχρωση, και το κόκκινο **μόνο** στη γραμμή της έκπτωσης — εκεί που σημαίνει
  κάτι. Οι γεμάτες μπάντες έγιναν τριχοειδείς γραμμές.
- **Εξώφυλλο**: τα δύο σήματα, μετά ένα βαθύ πλαίσιο με τον τίτλο και το προϊόν, και από
  κάτω ο πελάτης και τα στοιχεία σε καθαρό πλέγμα — αντί για το μπλε/κόκκινο δίπτυχο.
- **Τρέχουσα κεφαλίδα** σε κάθε σελίδα (σήμα + «Οικονομική Πρόταση · πελάτης») και
  **υποσέλιδο** με τον αριθμό πρωτοκόλλου και «ν / σύνολο».
- Οι αριθμοί ενοτήτων μπήκαν σε **πλακίδιο**· τα περιεχόμενα έχασαν τους στικτούς οδηγούς
  και έγιναν λίστα με τριχοειδείς γραμμές· η τελική αξία βγαίνει σε **σκούρο πλαίσιο**·
  ο τρόπος πληρωμής σε δύο κάρτες με τα ποσοστά σε μεγάλα ψηφία· οι τράπεζες σε κάρτες·
  το έντυπο αποδοχής σε πεδία με γραμμή αντί για κουτάκια πίνακα.
- Στον πίνακα ενοτήτων, το κενό κελί έγινε **άδειος δακτύλιος**: γεμάτη κουκκίδα = μέσα,
  δακτύλιος = εκτός. Το κενό άφηνε τον αναγνώστη να αναρωτιέται αν λείπει η πληροφορία.

**Διόρθωση: οι σελίδες ξεχείλιζαν.** Το έντυπο είχε **σταθερές** δεκατρείς σελίδες με
προκαθορισμένο τι μπαίνει πού. Με πολλά modules το περιεχόμενο ξεπερνούσε το φύλλο Α4 —
μια σελίδα τυπωνόταν σε δύο και η αρίθμηση έλεγε ψέματα. Τώρα οι πίνακες **ρέουν**: κόβονται
σε όσα κομμάτια χρειάζεται (με επανάληψη επικεφαλίδας και «συνέχεια»), οι σελίδες μετριούνται
στο τέλος, και τα **περιεχόμενα δείχνουν τις πραγματικές σελίδες**. Τα ύψη γραμμών είναι
μετρημένα σε χιλιοστά πάνω στο ίδιο το έντυπο (`MM_*` στην Pharmacy) — αν αλλάξει το στυλ
των πινάκων, αυτά ξαναμετριούνται.

**Έλεγχος**: και οι 4 εκδόσεις × ελαφριά/βαριά ρύθμιση — **0 σελίδες που ξεχειλίζουν** σε
όλες (14 σελίδες η ελαφριά, 16 η βαριά)· 36 οθόνες χωρίς σφάλμα JS.
- **Λογότυπα** ως στατικά αρχεία στο `projectmanagement/doc-assets/` — το `pharmacy_doc`
  καλείται σε κάθε αλλαγή πεδίου και δεν έχει νόημα να σέρνει base64 κάθε φορά. Μετά τη
  συγχώνευση Entersoft–SoftOne μπήκε το wordmark **ENTERSOFTONE** (`entersoftone.png`,
  909×115, διαφανές φόντο), χωρίς το παλιό «more than software». Ο κώδικας ψάχνει πρώτα το
  νέο αρχείο και πέφτει πίσω στο παλιό αν λείπει, ώστε να μη βγει ποτέ σπασμένη εικόνα σε
  προσφορά που φεύγει.

**Παράπλευρη διόρθωση: κλικ σε κάρτα προσφοράς ζητούσε task «NaN».** Οι κάρτες προσφορών
και leads δανείζονται τα class `.tcard` / `.kb-col` για την εμφάνιση, και το delegation του
`dnd()` είναι **καθολικό** — οπότε κάθε κλικ σε προσφορά καλούσε και `api('task&id=NaN')`
(404 σε κάθε κλικ), ενώ ένα σύρσιμο θα καλούσε `move_task` για ανύπαρκτη εργασία. Οι
επιλογείς έγιναν `.tcard[data-task]` / `.kb-col[data-status]`. Προϋπήρχε· φάνηκε στον
έλεγχο δικτύου αυτής της δουλειάς.

**Έλεγχος**: 36 οθόνες χωρίς σφάλμα JS και χωρίς 4xx· κύκλος δημιουργίας από άκρη σε άκρη
(κοστολόγηση → προσφορά → σήμανση στην κάρτα → επαναφορά στον κοστολογητή με τη ρύθμιση
ακέραιη → έγγραφο 7 σελίδων με τον σωστό πελάτη)· η δοκιμαστική προσφορά διαγράφηκε
(19 προσφορές, καμία `pharmacyone` σε παραγωγή μέχρι την πρώτη πραγματική).


## 4. Το πρότυπο πελάτη — `cloudon` (δικό μας fork)

**Από 30/07/2026 το ενεργό πρότυπο είναι το `templates/cloudon`**, fork του horn με
όλες τις προσαρμογές μας. Το `horn` δεν χρησιμοποιείται πλέον. Δες `templates/cloudon/CLOUDON-TEMPLATE.md`.

- **Το WHMCS δεν το αγγίζει ποτέ** (αντικαθιστά μόνο `six` / `twenty-one`).
- **Ο κατασκευαστής του horn δεν το αγγίζει ποτέ** — εκεί ήταν ο πραγματικός κίνδυνος.
- Το `templates/horn` μένει εγκατεστημένο και **ανέπαφο**, ως σημείο αναφοράς για
  να βλέπουμε τι αλλάζει ο κατασκευαστής σε κάθε νέα του έκδοση.

Δοκιμή χωρίς αλλαγή του ζωντανού:
`https://my.cloudon.gr/index.php?systpl=cloudon&carttpl=cloudon`

Οι παρακάτω προσαρμογές ισχύουν **και για τα δύο** πρότυπα (το cloudon τις έχει
κληρονομήσει). Όταν το `cloudon` γίνει προεπιλογή, το `horn` παύει να έχει σημασία.

### 4β. Ιστορικό: τι είχαμε αλλάξει στο horn

23 αρχεία του προτύπου έχουν δικές μας αλλαγές. Οι σημαντικότερες:

| Αρχείο | Τι αλλάξαμε | Αν χαθεί |
|---|---|---|
| `header.tpl` | Μετρητής καλαθιού (`cnp-cart-link`, `cnp-cart-badge`) | Χάνεται ο μετρητής |
| `assets/layout/menu.tpl` | Στοιχείο «Απομακρυσμένη υποστήριξη» | Χάνεται από το μενού |
| `login.tpl` | Δίγλωσσο, λογότυπο, προβολή εγγραφής | Επιστρέφει στα αγγλικά |
| `clientregister.tpl` | Νομική μορφή + άντληση ΑΑΔΕ | **Χάνεται η αυτόματη συμπλήρωση ΑΦΜ** |
| `css/custom.css` | Όλες οι προσαρμογές εμφάνισης + μετρητής καλαθιού | Σπάει η εμφάνιση |
| `css/tokens.css`, `css/foundation.css` | Χρώματα/τυπογραφία | Σπάει η εμφάνιση |
| `clientareahome.tpl`, `clientareaproducts.tpl` | Πίνακας ελέγχου, λίστα υπηρεσιών | Επιστρέφουν στο αρχικό |
| `viewticket.tpl`, `supportticket*.tpl` | Ροή αιτημάτων | Επιστρέφει στο αρχικό |
| `invoicepdf.tpl`, `quotepdf.tpl` | Μορφή PDF | Επιστρέφει στο αρχικό |
| `knowledgebase*.tpl` | Βάση γνώσης | Επιστρέφει στο αρχικό |

**Διαδικασία όταν έρθει νέα έκδοση του horn:**

1. `git commit` όλα πριν ξεκινήσεις — καθαρό working tree.
2. Αντίγραφο: `cp -a templates/horn templates/horn.backup-ΗΗΜΜΕΕ`
3. Εγκατάσταση της νέας έκδοσης του horn.
4. `git diff templates/horn` → δείχνει **ακριβώς** τι έσβησε ο κατασκευαστής.
5. Επανάφερε τις δικές μας αλλαγές μία-μία από το git.

Το git είναι εδώ ο σωτήρας: **κάθε δική μας αλλαγή είναι σε commit** με ελληνικό
μήνυμα που εξηγεί το γιατί.

---

## 5. Ρυθμίσεις βάσης που πρέπει να επαληθευτούν

Αυτές δεν είναι σε αρχεία — ζουν στη βάση και ενδέχεται να επανέλθουν σε
προεπιλογή μετά από αναβάθμιση.

### 5.1 Τρόποι πληρωμής

| Gateway | Ρυθμίσεις που πρέπει να υπάρχουν |
|---|---|
| `vivapayments` | `clientId`, `clientSecret`, `merchantId`, `apiKey`, `sourceCode` (**3425**), `environment=Παραγωγή`, `disableCash=on` |
| `banktransfer` | `instructions` (IBAN Alpha + Eurobank), όνομα |
| `paypal` | κανονικές ρυθμίσεις PayPal |

⚠️ Τα `clientSecret` και `apiKey` είναι **κρυπτογραφημένα**. Αν χαθούν, πρέπει να
ξαναμπούν από το Viva portal — το Client Secret **δεν ανακτάται**, θέλει νέα
δημιουργία credentials.

⚠️ Το πεδίο `instructions` της τραπεζικής κατάθεσης **δεν γράφεται από τη γραμμή
εντολών** — δοκιμάστηκαν 4 τρόποι. Μόνο μέσα από τη φόρμα του admin.

### 5.2 Αρίθμηση παραστατικών

| Ρύθμιση | Τιμή |
|---|---|
| `SequentialInvoiceNumbering` | `1` |
| `SequentialInvoiceNumberFormat` | `{YEAR}{NUMBER}` |
| `TaxCustomInvoiceNumbering` | `1` |
| `TaxCustomInvoiceNumberFormat` | `PF{YEAR}{NUMBER}` |

⚠️ **Οι μετρητές (`SequentialInvoiceNumberValue`, `TaxNextCustomInvoiceNumber`)
είναι λογιστικά κρίσιμοι.** Σημείωσέ τους πριν την αναβάθμιση και επιβεβαίωσέ τους
μετά. Αν πάνε πίσω, θα εκδοθούν διπλοί αριθμοί παραστατικών.

### 5.3 Λοιπά

| Ρύθμιση | Τιμή |
|---|---|
| `Template` / `OrderFormTemplate` | `cloudon` / `cloudon` (fork μας· επιστροφή: `horn`) |
| `Language` | `greek` |
| `TaxEnabled` | `on` (ΦΠΑ 24%, τύπος Exclusive) |
| `AutoRedirectoInvoice` | `gateway` |

### 5.4 Cron

```
*/5 * * * * (/opt/plesk/php/8.3/bin/php -f '.../sys_cron_k7m2/cron.php') > /dev/null
```
Χρήστης: `cloudon.gr_chbouao8z8` (Plesk). Ο φάκελος είναι **μετονομασμένος** —
αν η αναβάθμιση επαναφέρει `crons/`, διόρθωσε τη γραμμή.

---

## 5β. Ο παλμός — πώς πιάνουμε τη σιωπηλή βλάβη

Ο έλεγχος αρχείων δείχνει ότι ο κώδικας **υπάρχει**. Δεν δείχνει ότι **τρέχει**.

Αυτή είναι η επικίνδυνη περίπτωση μετά από αναβάθμιση: αν το WHMCS μετονομάσει ή
αλλάξει ένα hook, ο δικός μας κώδικας μένει στη θέση του, **δεν βγάζει σφάλμα**,
απλώς δεν καλείται ποτέ. Οι πληρωμές Viva σταματούν να καταχωρούνται, η χρέωση
PayPal παύει να μπαίνει — και δεν το προσέχει κανείς για μέρες.

Το `includes/hooks/cnp_heartbeat.php` βάζει χρονοσήμανση **μέσα στους δικούς μας
χειριστές** (όχι σε ξεχωριστά hooks), ώστε να αποδεικνύεται ότι εκτελέστηκε ο
κώδικάς μας. Κόστος: μία εγγραφή ανά hook ανά 10 λεπτά, όχι ανά αίτημα.

| Παλμός | Πότε χτυπά | Όριο σιωπής |
|---|---|---|
| `clientarea` | κάθε επίσκεψη πελάτη | 24 ώρες |
| `cron` | κάθε 5 λεπτά | 2 ώρες |
| `viva_reconcile` | κάθε cron | 2 ώρες |
| `cart_footer` | στο ταμείο | 14 ημέρες |
| `invoice_created` | νέο τιμολόγιο | 7 ημέρες |
| `gateway_change` | αλλαγή τρόπου πληρωμής | 30 ημέρες |
| `invoice_paid` | εξόφληση | 7 ημέρες |

**Πώς διαβάζεται μετά την αναβάθμιση:** τρέξε `healthcheck.php` **πριν**, κράτα το
αποτέλεσμα. Μετά την αναβάθμιση περίμενε λίγες ώρες φυσιολογικής κίνησης και
ξανατρέξ' το. Παλμός που ήταν πράσινος και τώρα λέει **ΣΙΩΠΑ** = το hook έσπασε.

Το «δεν έχει χτυπήσει ποτέ ακόμη» είναι φυσιολογικό για τα σπάνια γεγονότα μέχρι
να συμβούν για πρώτη φορά.

---

## 6. Λίστα ελέγχου μετά την αναβάθμιση

Με σειρά προτεραιότητας. Τα 🔴 είναι αυτά που κοστίζουν χρήματα ή σπάνε πωλήσεις.

### 🔴 Παλμοί — μετά από λίγες ώρες κίνησης

- [ ] `healthcheck.php` → κανένας παλμός δεν λέει **ΣΙΩΠΑ**;
- [ ] Σύγκριση με το `health-before.txt`: κάτι που ήταν πράσινο έγινε κόκκινο;

### 🔴 Πληρωμές — έλεγξε πρώτα

- [ ] Άνοιξε ανεξόφλητο τιμολόγιο → εμφανίζονται και οι 3 μέθοδοι πληρωμής;
- [ ] Επίλεξε **Viva** → πάει στη σελίδα πληρωμής της Viva με λογότυπο CloudOn;
- [ ] Πλήρωσε **1 €** δοκιμαστικά → το τιμολόγιο γίνεται Paid μέσα σε δευτερόλεπτα;
- [ ] Στο ταμείο, επίλεξε **PayPal** → ανεβαίνει το σύνολο με τη χρέωση διεκπεραίωσης;
- [ ] Ολοκλήρωσε παραγγελία με PayPal → το τιμολόγιο έχει **μία** γραμμή χρέωσης και σωστό σύνολο;
- [ ] Δοκίμασε **επιστροφή χρημάτων** από το admin (κουμπί Refund).

Αν οι πληρωμές Viva δεν καταχωρούνται:
```
/opt/plesk/php/8.3/bin/php modules/gateways/vivapayments/crons/reconcile.php
```
Αν αυτό δουλεύει αλλά η αυτόματη καταχώρηση όχι → έσπασε το `viva_reconcile.php`
(πιθανότατα άλλαξε το hook `ClientAreaPage` ή `AfterCronJob`).

### 🔴 Παραγγελίες

- [ ] Πρόσθεσε προϊόν στο καλάθι → εμφανίζεται ο **μετρητής** στο εικονίδιο;
- [ ] Ολοκλήρωσε παραγγελία → γίνεται **αυτόματη αποδοχή**; (`hz_auto_accept_orders`)
- [ ] Δημιουργείται η υπηρεσία στο Hetzner;

### 🟠 Εγγραφή & γλώσσα

- [ ] Φόρμα εγγραφής: εμφανίζεται η επιλογή **Ιδιώτης / Επιχείρηση**;
- [ ] Με ΑΦΜ επιχείρησης → **συμπληρώνονται αυτόματα** επωνυμία/διεύθυνση/ΔΟΥ;
- [ ] Εναλλαγή EL/EN στη σύνδεση δουλεύει;
- [ ] Τα custom fields εμφανίζονται στα ελληνικά;

### 🟠 Μενού & σελίδες

- [ ] Στο μενού πελάτη υπάρχει **«Απομακρυσμένη υποστήριξη»**;
- [ ] `https://my.cloudon.gr/remote` φορτώνει και κατεβάζει ZIP;
- [ ] `https://my.cloudon.gr/project/` (Project Management) φορτώνει;

### 🟠 Προσλήψεις

- [ ] `https://my.cloudon.gr/project/apply.php` φορτώνει με τις ενεργές θέσεις;
- [ ] Άνοιξε μια αγγελία → **αυξάνεται** ο μετρητής στο Προσλήψεις → Επισκεψιμότητα;
- [ ] Η υποβολή αίτησης με CV δουλεύει (αποθήκευση στο S3);

### 🟡 Εμφάνιση

- [ ] Πίνακας ελέγχου πελάτη με τα πλακίδια;
- [ ] Λίστα υπηρεσιών με εικονίδια και σωστές καταστάσεις;
- [ ] Εμφάνιση σε κινητό (μενού, καλάθι, τιμολόγια);
- [ ] PDF τιμολογίου με το σωστό λογότυπο και στοιχεία;

### ⚪ Τεχνικά

- [ ] `SequentialInvoiceNumberValue` και `TaxNextCustomInvoiceNumber` **δεν πήγαν πίσω**;
- [ ] Το cron τρέχει (Utilities → System → System Health);
- [ ] Δεν υπάρχουν σφάλματα στο `/var/www/vhosts/system/my.cloudon.gr/logs/error_log`;

---

## 7. Πριν την αναβάθμιση — υποχρεωτικά

```bash
cd /var/www/vhosts/cloudon.gr/my.cloudon.gr

# 1. Καθαρό git
git status                    # πρέπει να είναι clean
git add -A && git commit -m "Πριν την αναβάθμιση WHMCS" && git push

# 2. Αντίγραφο βάσης
mysqldump -u USER -p DBNAME | gzip > /root/whmcs-pre-upgrade-$(date +%F).sql.gz

# 3. Αντίγραφο αρχείων που δεν είναι στο git
cp -a configuration.php /root/configuration.php.bak
crontab -l -u cloudon.gr_chbouao8z8 > /root/crontab-pre-upgrade.bak

# 4. Κατέγραψε τους μετρητές παραστατικών
/opt/plesk/php/8.3/bin/php -r 'require "init.php";
 foreach (["SequentialInvoiceNumberValue","TaxNextCustomInvoiceNumber"] as $s)
  echo "$s = ".WHMCS\Database\Capsule::table("tblconfiguration")->where("setting",$s)->value("value")."\n";'

# 5. Έλεγχος υγείας ΠΡΙΝ, για σύγκριση
/opt/plesk/php/8.3/bin/php scripts/healthcheck.php > /root/health-before.txt
```

---

## 7β. Αμέσως μετά την αναβάθμιση

```bash
cd /var/www/vhosts/cloudon.gr/my.cloudon.gr

# 1. Τι έσβησε η αναβάθμιση — το git το δείχνει ακριβώς
git status
git diff --stat

# 2. Αυτόματος έλεγχος και σύγκριση με το πριν
/opt/plesk/php/8.3/bin/php scripts/healthcheck.php > /root/health-after.txt
diff /root/health-before.txt /root/health-after.txt

# 3. Ό,τι έσβησε, επαναφορά από το git (ΠΡΟΣΟΧΗ: μόνο δικά μας αρχεία)
git checkout -- includes/hooks/ templates/horn/ lang/overrides/

# 4. Καθάρισε τα μεταγλωττισμένα πρότυπα
rm -f tpl_cache_k7m2/*.php

# 5. Ξανατρέξε τον έλεγχο — πρέπει 0 σφάλματα
/opt/plesk/php/8.3/bin/php scripts/healthcheck.php
```

**Αν το `git status` δείχνει διαγραμμένα δικά μας αρχεία** → η αναβάθμιση τα έσβησε.
Το `git checkout` τα επαναφέρει ακέραια. Γι' αυτό το βήμα «καθαρό git πριν» είναι
υποχρεωτικό: χωρίς αυτό δεν ξεχωρίζεις τι έσβησε η αναβάθμιση από τι δεν είχες σώσει.

---

## 8. Γνωστές παγίδες αυτής της εγκατάστασης

Πράγματα που μας κόστισαν ώρες. Μην τα ξαναψάξεις.

| Παγίδα | Λεπτομέρεια |
|---|---|
| **Το horn δεν χρησιμοποιεί τον navbar του WHMCS** | Το μενού είναι hardcoded στο `assets/layout/menu.tpl`. Τα hooks `ClientAreaPrimaryNavbar` **δεν κάνουν τίποτα**. |
| **Το horn δεν εμφανίζει προσαρμογές καλαθιού** | Το `CartTotalAdjustment` αλλάζει το σύνολο **αόρατα**. Γι' αυτό η χρέωση PayPal γίνεται με JavaScript. |
| **`updateInvoiceTotal()` δεν ξαναϋπολογίζει το υποσύνολο** | Γραμμές που αθροίζουν 11,92 άφηναν υποσύνολο 10,79. Χρησιμοποιούμε δικό μας `cnp_invoice_recalc()`. |
| **`encrypt()` παράγει μορφή `2$…`** | Το `getGatewayVariables()` διαβάζει μόνο την παλιά μορφή `hex.hex`. Πεδία gateway **δεν γράφονται από CLI**. |
| **Η βάση είναι utf8mb3** | **Μην αποθηκεύεις emoji** — γίνονται `????`. Μόνο κείμενο. |
| **`preg_split` χωρίς `/u`** | Κόβει σε **bytes** και σπάει τα ελληνικά (το `·` συγκρούεται με το «η»). |
| **Το radio του ταμείου δεν πυροδοτεί `change`** | Το πρότυπο το επιλέγει μέσω JS. Η χρέωση PayPal ελέγχει με χρονομετρητή. |
| **Cron ανά λεπτό ρίχνει τον server** | Δοκιμάστηκε: φορτίο 15, cron στο 75% CPU. **Μην βάλεις ξανά.** |
| **Endpoints Viva** | Δημιουργία παραγγελίας → `api.vivapayments.com`. Επιστροφές, κατάσταση παραγγελίας, κλειδί webhook → `www.vivapayments.com` με Basic auth. |
| **Πηγή πληρωμής Viva μέσω API** | Δημιουργείται ελλιπής — **δεν δέχεται λογότυπο**. Φτιάξ' την από τη φόρμα του portal. |

---

**Ανασχεδιασμός του task drawer γύρω από τη συνομιλία** (4/9/2026). Ο χειριστής ζει
μέσα στην εσωτερική επικοινωνία της εργασίας, αλλά αυτή ήταν χωμένη κάτω από τη φόρμα και
τα βοηθητικά — σε εργασία με εκατοντάδες μηνύματα ήταν αδύνατο να βρεις κάτι. Αναποδογύρισε:

- **Αριστερά, πλατιά, το κέντρο: μόνο η συνομιλία.** Κεφαλίδα με **ζωντανή αναζήτηση**
  (χωρίς τόνους — «Νικος» βρίσκει «Νίκος»), **φίλτρα** (Όλα / Προς εμένα / Με αρχεία) και
  μετρητή «X / σύνολο». Η ροή κυλά μόνη της με **διαχωριστικά ημέρας** (Σήμερα/Χθες/…),
  avatar σταθερού χρώματος ανά ομιλητή, «→ παραλήπτης», και ο συνθέτης καρφωμένος κάτω.
  Ανοίγει στα **πιο πρόσφατα** μηνύματα· κουμπί «↓ Νεότερα» όταν έχεις κυλήσει πάνω.
- **Δεξιά, στήλη που κυλά ανεξάρτητα: ΟΛΕΣ οι πληροφορίες του έργου** — τίτλος/πεδία,
  «το ζητούμενο», χρόνος, εξαρτήσεις, αρχεία, ενέργειες (checklist), ticket, ιστορικό.
- Η αναδιάταξη γίνεται σε JS μετά το render (η κάρτα `.tk-conv` → αριστερά, όλες οι άλλες
  → δεξιά)· η σειρά του DOM μένει λογική, ώστε σε κινητό να στοιβάζεται σωστά (συνομιλία
  πρώτη, μετά οι πληροφορίες). Κανένα `id` handler δεν άλλαξε — μόνο η διάταξη και το
  markup της συνομιλίας.

Έλεγχος: με 46 δοκιμαστικά μηνύματα (καθαρίστηκαν) — αναζήτηση «sql» → 8/46 με σωστά
διαχωριστικά ημέρας, φίλτρο «προς εμένα» → 35, «με αρχεία» → κενή κατάσταση· άνοιγμα στα
πρόσφατα· 36 οθόνες × χωρίς σφάλμα JS· κινητό στοιβάζεται σωστά.

*Διόρθωση διάταξης (ίδια μέρα):* η στενή δεξιά στήλη ξεχείλιζε οριζόντια — τα `date`
inputs έχουν μεγάλο εγγενές πλάτος και σε δύο στήλες χωρίς `min-width:0` έσπρωχναν τη
στήλη, με αποτέλεσμα να κόβονται πεδία/κουμπιά και να βγαίνει οριζόντια μπάρα. Το
παράθυρο μεγάλωσε (`min(1340px,96vw)` × `min(90vh,1000px)`), η δεξιά στήλη πήρε
`clamp(360px,40%,560px)` με `overflow-x:hidden`, τα πεδία της (`.frow`) έγιναν
`auto-fit/minmax(185px,1fr)` με `min-width:0` στα κελιά, και η γραμμή ενεργειών
αναδιπλώνεται. Επαληθεύτηκε: μηδέν οριζόντια υπερχείλιση σε 1280/1440/1512 και σε κινητό.

**Συνημμένα chat: κάθε τύπος αρχείου, με ασφάλεια** (4/9/2026). Ο χειριστής ήθελε να
στέλνει Excel, PDF, **πηγαίο κώδικα** και κάθε άλλο αρχείο. Excel/PDF ήδη περνούσαν
(η `cnp_file_ext_ok` ήταν denylist), αλλά ο κώδικας (`.php .js .py .sh .rb …`), html και
svg ήταν μπλοκαρισμένα — σωστά, γιατί το local storage είναι κάτω από το docroot κι ένα
`.php` θα εκτελούνταν. Αντί να χαλαρώσω απλώς τη λίστα (τρύπα web-shell/XSS), μετακίνησα
την ασφάλεια σε **τρία επίπεδα** και άνοιξα τους τύπους:

- **Ο φάκελος αποθήκευσης** (`attachments/cloudon-storage`) είναι ήδη `Deny from all` και
  **δεν εκτελεί PHP** — επαληθεύτηκε ζωντανά (άμεσο GET → 403, `<?php>` δεν τρέχει).
- **Τοπικά αρχεία** γράφονται στον δίσκο με **ουδέτερη κατάληξη `.bin`** (`Storage::store`)·
  η πραγματική κατάληξη & ο τύπος ζουν στο μητρώο και μπαίνουν στο κατέβασμα. Έτσι κανένα
  αρχείο δεν μπορεί να εκτελεστεί/σερβιριστεί ως ενεργό, ακόμη κι αν αύριο αλλάξει το vhost.
- **Το `file_get` σερβίρει inline ΜΟΝΟ παθητικούς τύπους** (`cnp_mime_inline_ok`: pdf,
  εικόνες εκτός svg, ήχος/βίντεο)· κώδικας/html/svg φεύγουν πάντα ως **attachment** με
  `nosniff` — και για local και για S3 (presigned με `ResponseContentDisposition=attachment`).
  Έτσι το svg (που εκτελεί script) δεν σερβίρεται ποτέ inline → κλείνει το stored-XSS.

Η `cnp_file_ext_ok` επιστρέφει πλέον πάντα `true` (η ασφάλεια δεν στηρίζεται στην κατάληξη).
Έλεγχος: ανέβηκαν php/py/js/svg/html/xlsx/pdf/Makefile — όλα αποθηκεύτηκαν· `.js`/`.py`/`.svg`/
`.html` → 302 με `attachment` (λήψη), `.pdf`/`.png` → inline· δοκιμή από τον ίδιο τον composer
του chat (4 αρχεία μαζί) OK· δοκιμαστικά καθαρίστηκαν (σχόλια + αντικείμενα S3).

**Παράπλευρη διόρθωση: «Διαχειριστές (όλοι)» στο chat έβγαζε 400.** Ο συνθέτης πρόσφερε
παραλήπτη «όλοι» (τιμή **-1**) και η προβολή/`Notify::commentTo`/τα δεδομένα το υποστήριζαν
(broadcast σε full-access admins), αλλά η ενέργεια `comment` απέρριπτε `to <= 0`, κόβοντας το
έγκυρο -1. Πλέον απορρίπτεται μόνο το «κανένας» (κενό/0)· το -1 γίνεται δεκτό.

**«Ζήτα βοήθεια» — επείγουσα έκκληση σε συνάδελφο** (4/9/2026). Ένας εμπλεκόμενος μπορεί
να ζητήσει ζωντανά τη βοήθεια συναδέλφου: «κάλεσέ με, θέλω να το συζητήσουμε». Δεν είναι
task ούτε ticket — είναι προσωπική έκκληση που **«κάνει μπαμ»** στην οθόνη του παραλήπτη.

- **Είσοδος**: κουμπί «🆘 Βοήθεια» στο task drawer (με προ-συμπληρωμένο «Σχετικά με» το
  task), και «+ Νέο → Ζήτα βοήθεια» (χωρίς task). Διάλογος: συνάδελφος + μήνυμα (με έτοιμο
  πρότυπο). Διαθέσιμο σε όλους — συνεργασία, όχι δικαίωμα.
- **Το «μπαμ»**: μόλις το `version` (poll 12") φέρει ανοιχτή έκκληση προς εμένα, ανοίγει
  **κεντρικό κόκκινο modal** με παλλόμενο 🆘, το όνομα, το μήνυμα και το σχετικό task —
  με ήχο (WebAudio) και **αναβοσβήνει ο τίτλος της καρτέλας** («🆘 Βοήθεια!») ώστε να
  φανεί ακόμη κι από άλλη καρτέλα. Κουμπιά: «Άνοιξε το θέμα» / «Το είδα — καλώ τώρα».
- Μπαίνει και στο **καμπανάκι** (μένει ώσπου να διαβαστεί) και στέλνεται **email**. Το
  `seen_at` σημειώνεται μόλις δειχθεί το popup, ώστε να μη ξαναχτυπήσει σε επόμενο poll.
- Νέος πίνακας `mod_cpm_help` (from/to/task/message/status/seen_at/done_at), ενέργειες
  `help_ask` / `help_seen` / `help_done`, `Notify::helpAsked`, και `alerts` στο `version`.

*Παράπλευρα:* το ίδιο poll δείχνει τις εκκλήσεις **πριν** από τους φραγμούς «μη ενοχλείς
όσο γράφει / με ανοιχτό drawer» — μια έκκληση βοήθειας πρέπει να περνά πάντα.

**Φωνή ομάδας (Discord-style) — μόνιμο δωμάτιο + «έλα τώρα»** (5/9/2026). Πάνω στην
υπάρχουσα μηχανή **CloudOn Meet** (WebRTC mesh audio/video/screen), όχι από το μηδέν.

- **Μόνιμο δωμάτιο** `mteamvoice`: μπαινοβγαίνεις ελεύθερα, χωρίς προγραμματισμό. Μπάρα
  «🔊 Φωνή ομάδας» πάνω από το chat (Η ομάδα) με **παρουσία** (ποιος είναι μέσα τώρα —
  avatars + μετρητής, poll `voice_presence` ανά 10"), κουμπί «🎙 Μπες στη φωνή» (ανοίγει
  το Meet — οι logged-in δεν χρειάζονται token) και «🔔 Κάλεσε την ομάδα».
- **«Έλα τώρα»** (`voice_call`): στέλνει το ίδιο δυνατό «μπαμ» με το «Ζήτα βοήθεια», σε
  **μπλε** παραλλαγή («🔊 X σε καλεί στη φωνή» + «Μπες στη φωνή»). Στήθηκε πάνω στο
  `mod_cpm_help` με νέα στήλη `kind` (help|voice)· τα `alerts` του `version` φέρουν `kind`.
- **Ρυθμιζόμενο ICE**: το `meet.php` διαβάζει STUN/TURN από ρυθμίσεις του addon
  (`ice_stun`, `ice_turn`, `ice_turn_user`, `ice_turn_pass`)· fallback σε Google STUN.
  Έτσι δουλεύει **σε LAN τώρα**, και μόλις μπει TURN ανοίγει **από παντού** χωρίς αλλαγή
  κώδικα — αρκεί να συμπληρωθούν τα κλειδιά.

**✅ TURN server (5/9/2026).** Στήθηκε **coturn** στο `remote.cloudon.gr` (78.47.174.128,
ίδιο κουτί με RustDesk/Guacamole) με idempotent script — δεν αγγίχτηκε το RustDesk. Οι
ρυθμίσεις `ice_turn/ice_turn_user/ice_turn_pass` μπήκαν στο addon και το `meet.php` τις
σερβίρει (UDP + TCP transport). Επαληθεύτηκε **STUN Binding Success μέσω UDP 3478 από
εξωτερικό δίκτυο** — η φωνή δουλεύει πλέον και εκτός LAN. Mesh P2P: άνετα ~5-6 άτομα/
δωμάτιο· παραπάνω θέλει SFU (μεγαλύτερο έργο). *Σημ.:* αν μπει Hetzner Cloud Firewall,
θέλει inbound UDP 3478, TCP 3478, UDP 49160-49200.

**Push notifications στη συσκευή (Web Push / VAPID)** (5/9/2026). Μέχρι τώρα κάθε
ειδοποίηση (ανάθεση, σχόλιο, «ζήτα βοήθεια», «έλα στη φωνή», SLA, deadline) έφτανε **μόνο
με ανοιχτή καρτέλα** (poll 12"). Πλέον φτάνει στο **κινητό/desktop ακόμη κι με κλειστή
εφαρμογή**.

- **Χωρίς payload encryption**: στέλνουμε «κενό» push (VAPID ES256, καθαρός PHP+openssl —
  όχι aes128gcm)· ο service worker ξυπνά και **τραβά το περιεχόμενο** από `push_latest`
  (τίτλος + SPA deep-link). Έτσι το tap ανοίγει κατευθείαν το σωστό task/οθόνη.
- **`lib/Push.php`**: VAPID JWT ανά origin (DER→raw υπογραφή, επαληθεύτηκε με `openssl_verify`),
  αποστολή σε όλες τις συσκευές του χρήστη, κλάδεμα νεκρών συνδρομών (404/410). Κλειδιά
  VAPID στις ρυθμίσεις (`vapid_public/private/subject`).
- **`mod_cpm_push_subs`** (endpoint/p256dh/auth ανά admin). Ενέργειες `push_pubkey`,
  `push_subscribe`, `push_unsubscribe`, `push_latest`. Σύνδεση στο `Db::pushNotification`
  (fire-and-forget, ποτέ blocking — try/catch + σύντομα timeouts).
- **Frontend**: ο SW (`sw.js`) απέκτησε `push` + `notificationclick`. Στο SPA:
  `cnpPushInit()` (ξανα-συγχρονίζει τη συνδρομή αν υπάρχει ήδη άδεια) + link **«🔔
  Ενεργοποίηση ειδοποιήσεων σε αυτή τη συσκευή»** μέσα στο καμπανάκι (δεν ρωτάμε αυτόματα).
  Το tap κάνει πλοήγηση στην ήδη ανοιχτή καρτέλα μέσω `postMessage`.

Έλεγχος: VAPID JWT verify=1· push_pubkey/subscribe/latest OK· SW εγγράφεται· link
ενεργοποίησης εμφανίζεται· 36 οθόνες χωρίς σφάλμα. **Η πραγματική παράδοση σε κινητό
δοκιμάζεται από συσκευή** (χρειάζεται FCM/πραγματικό push service — δεν γίνεται headless).
*Follow-up αν χρειαστεί:* ουρά αποστολής, ώστε ένα broadcast «έλα στη φωνή» σε 10 άτομα να
μη σειριοποιεί 10 κλήσεις FCM στο ίδιο request.

## 9. Ανοιχτά θέματα (Αύγουστος 2026)

- **Staging**: το `mystaging.cloudon.gr` είναι σε **WHMCS 8.11.2** με δική του βάση
  (`mystagingdb`) — δύο εκδόσεις πίσω και χωρίς τίποτα από όσα φτιάξαμε. **Πρέπει να
  γίνει πιστό αντίγραφο της παραγωγής** (ίδιος κώδικας, ίδια βάση, πρότυπο `cloudon`)
  για να έχει νόημα ως πρόβα αναβάθμισης. Κατά την αντιγραφή πρέπει να εξουδετερωθούν:
  αποστολή email, cron, κλειδιά πληρωμών (Viva σε Demo), και **τα server modules
  Hetzner/Scaleway** — αλλιώς μια δοκιμή μπορεί να τερματίσει πραγματικούς servers.

- **Webhook Viva**: δεν φτάνει ποτέ. Πιθανή αιτία: ο λογαριασμός Viva είναι κοινός
  με το RxVision, το οποίο κρατά τη θέση webhook. Δεν είναι κρίσιμο — η εξόφληση
  γίνεται σε 1-2" από τον άμεσο έλεγχο.
- **Scaleway**: το module είναι έτοιμο αλλά **δεν έχει δοκιμαστεί με πραγματικά
  κλειδιά**. Ο addon δεν είναι ενεργοποιημένος.
- **Οδηγίες τραπεζικής κατάθεσης**: παραμένουν στα αγγλικά. Θέλουν επικόλληση
  δίγλωσσου κειμένου από τη φόρμα του admin.
- **Email ειδοποιήσεων PM**: η ρύθμιση `notify_email` του `cloudonprojects` είναι
  **κενή**, άρα οι ειδοποιήσεις μένουν μόνο μέσα στην εφαρμογή. Η κλιμάκωση
  εκπρόθεσμων προθεσμιών στον υπεύθυνο **δεν στέλνει email** μέχρι να ανοίξει.
- **Δεδομένα πωλήσεων**: τα leads είναι **μηδέν**· ο μηχανισμός (πεδία, scoring,
  εισαγωγή CSV, διπλότυπα, καμπάνιες) υπάρχει αλλά χωρίς δεδομένα. Οι 18
  προσφορές υιοθετήθηκαν από τα WHMCS quotes και έχουν όλες παρελθοντικές
  ημερομηνίες.
- **Προθεσμίες χωρίς δεδομένα**: κανένα έργο δεν έχει `due_date` ούτε υπεύθυνο,
  και μία μόνο εργασία έχει ημερομηνία λήξης — το κύκλωμα κλιμάκωσης δεν έχει
  τι να ειδοποιήσει.
- **Ποσοστό PayPal**: ρυθμισμένο σε 5,4% + 0,35 € από παλινδρόμηση σε 27
  συναλλαγές. Χρειάζεται επαλήθευση με πραγματικές πληρωμές — αν το πραγματικό
  κόστος είναι χαμηλότερο, **πρέπει να μειωθεί** (η προσαύξηση δεν επιτρέπεται
  να ξεπερνά το κόστος).
