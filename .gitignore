# ── Secrets (NEVER commit) ─────────────────────────────
/configuration.php
/.htpasswd
*.key
*.pem

# ── Ephemeral: sessions, caches, compiled templates ────
/phpsessions/
/templates_c/
**/templates_c/
/tpl_cache_k7m2/
/sys_cron_k7m2/
*.log
/logs/
/attachments/index.php.cache

# ── Third-party / vendor (ionCube-encrypted, not our code)
/vendor/
/whmcs/

# ── Customer data / uploads (privacy + size) ───────────
/attachments/
/attachmentsnew/
/downloads/
/logiscoopFiles/

# ── Build / OS ─────────────────────────────────────────
node_modules/
.DS_Store
Thumbs.db
*.swp

# ── Runtime symlinks / ACME / verification ─────────────
/project
/.well-known/acme-challenge/
/*.txt
b4e0f6955f975064a74c1b16cd06a9a9.txt

# ── Debug (προσωρινό — αφαιρείται όταν λυθεί το Save products) ──
/includes/hooks/zz_debug_productsave.php

# ── Runtime caches που αλλάζουν μόνα τους (θόρυβος στο auto-backup) ──
**/cache/availability.json
/modules/addons/hetznercloud/cache/
